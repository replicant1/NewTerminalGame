WI-3  START   04:31:46Z  DEV-C taking WI-3 (the window and its event loop) on branch r6/wi-3-window-event-loop, based on main @1f6ea32. Non-local mode. Reading plan, architecture and requirements before touching anything.
WI-3  READ    04:31:56Z  docs/FUNCTIONAL_REQUIREMENTS.md (49 codes) and docs/ARCHITECTURE.md (candidate 2 is the adopted one). Noted A1 (title), A2 (WIN-4 anchor needs Accessibility), A3 (WIN-5 vs END-5/END-6 contradiction), C7 (reap on failure path).
WI-3  READ    04:32:49Z  docs/IMPLEMENTATION_PLAN.md whole. WI-3 = Shell only: own native window (told its pixel size), title, black, non-resizable, fixed offset placement, ~7Hz tick timer, raw key delivery, idempotent end-of-session that reaps on the failure path. The automated suite must never construct a toolkit window.
WI-3  VERIFY  04:32:52Z  /usr/bin/python3 -> Python 3.9.6; tkinter reports Tk 8.5 / Tcl 8.5. Bare-tree suite command -> Ran 0 tests, OK.
WI-3  PLAN    04:32:57Z  Shell package terminal_game/shell/: a toolkit seam (abstract Toolkit + plain KeyPress value + WindowSpec), a Tk adapter that is the only module importing tkinter, a TickTimer that re-arms a one-shot after() at 143ms, and a WindowOwner composing them. Every WI-3 test drives WindowOwner/TickTimer through a recording double; no test constructs a Tk window.
WI-3  VERIFY  04:33:01Z  ls-remote for r6/wi-3-* returned empty; the remote carries no r6/ branches at all yet. Branch r6/wi-3-window-event-loop cut from main @1f6ea32.
WI-3  WEIGH   04:33:38Z  repeating tick: a toolkit-native repeat vs a one-shot after() the TickTimer re-arms itself : Tk only offers one-shot after(), and re-arming is real Shell logic worth owning and testing.
WI-3  DECIDE  04:33:40Z  tick timer -> a TickTimer that delivers the tick and only then re-arms a one-shot, because a tick whose collaborator raised must not leave a callback scheduled against a half-dead session.
WI-3  DECIDE  04:33:43Z  key delivery -> the Tk adapter normalises Tk's event object into a plain KeyPress(keysym, char) at the seam and the owner hands that on untouched, because WI-9's translator sits in Presentation and may not name the toolkit.
WI-3  DECIDE  04:33:44Z  collaborator exception -> reap the window, then re-raise, because swallowing it would hide a defect behind a window that merely vanished.
WI-3  TEST    04:35:17Z  50 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"]  — shell seam, Tk adapter, tick timer and window owner. No window constructed.
WI-3  COMMIT  04:36:01Z  50565d4 WI-3: the window, its tick timer and key delivery
WI-3  NOTE    04:36:16Z  Pushed r6/wi-3-window-event-loop and opened draft PR #25 -> https://github.com/replicant1/NewTerminalGame/pull/25 (base main).
WI-3  RISK    04:36:23Z  About to run tools/probe_tk_window.py, which puts ONE real Tk window on the user's screen for ~1.2s. Quit is scheduled on Tk's own scheduler before the loop is entered; reap is in a finally; an external runner SIGKILLs at 15s as a backstop. The window belongs to this process, so a kill takes the window with it - there is no second application left holding one, and so no modal sheet.
WI-3  VERIFY  04:36:49Z  Probe run 1: one Tk window opened and closed by itself, exit 0, lifetime 1.382s. 8 ticks delivered, observed interval 144.2ms against a nominal 143ms - GHOST-1 holds on real Tk 8.5.
WI-3  VERIFY  04:36:53Z  Probe run 1 also found two things by accident. (a) Tk 8.5 swallows an exception raised inside an after() callback: it goes to report_callback_exception, prints a traceback and mainloop carries on - so WindowOwner's re-raise would NOT escape run(). The reap still happened because the owner reaps before re-raising. (b) tkinter's root.resizable() with no arguments returns the STRING '0 0', not a pair, so unpacking it raises.
WI-3  VERIFY  04:36:55Z  No probe process left behind: pgrep -fl probe_tk_window is empty. The only Python process on the box is the orchestration server.
WI-3  DECIDE  04:36:58Z  Tk's swallowed callback exception -> wire root.report_callback_exception to the adapter, remember the first exception, stop the loop, and re-raise it out of run_event_loop, because otherwise the seam's contract ('a collaborator that raises loses its window AND the failure is visible') is true of the double and false of the real thing.
WI-3  VERIFY  04:38:00Z  Probe run 2 (normal path): title read back 'Terminal Game' EXACTLY - nothing composed around it, which is the candidate-1 shortfall the architect measured at V4 closing under candidate 2. Geometry exact: asked 512x600 at (140,140), measured window 512x600 at (140,140), canvas 512x600. Non-resizable on both axes. Background black. Tk 8.5.9. 8 ticks at 144.2ms. window_reaped true, exit 0, lifetime 1.386s.
WI-3  VERIFY  04:38:01Z  Probe run 3 (--fail, the failure path with a real window): the deliberate exception came back out of owner.run(), the window was reaped (window_reaped true), the session ended at the failure at 900ms rather than at the 1200ms backstop (lifetime 1.055s, 6 ticks), and the process exited by itself. The report_callback_exception wiring works end to end.
WI-3  ASK     04:38:05Z  WIN-3 / assumption A1: does the TITLEBAR read exactly 'Terminal Game'? I measured that Tk reports the title back unchanged, which is strictly stronger than the architect's candidate-1 V4 measurement, but reading a string back from the toolkit that set it is not a person seeing the titlebar. Only the user can answer.
WI-3  ASSUME  04:38:06Z  Proceeding on A1 unchanged: the titlebar reads 'Terminal Game'. Nothing in WI-3 changes either way - the title is one constant, WINDOW_TITLE in window_owner.py. Blast radius if the answer is bad: that constant, and the WIN-3 rows of the WI-20 sweep. The plan asks for the human look at WI-4 and WI-21.
WI-3  TEST    04:38:26Z  54 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"]  — plus the four covering the swallowed-callback bookkeeping.
WI-3  COMMIT  04:38:37Z  54b65eb WI-3: make Tk stop swallowing a callback that fell over
WI-3  NOTE    04:39:12Z  DEV-B's r6/wi-1-picture-value is on origin and independently chose the same top-level package (terminal_game) and the same tests/ root. Two files were going to collide on merge - terminal_game/__init__.py and tests/__init__.py - both pure docstrings. Adopted DEV-B's text for both VERBATIM on this branch, so the two branches now add identical content at those paths and merge without a conflict. Their text is the fuller of the two; nothing functional crossed the boundary.
WI-3  COMMIT  04:39:38Z  8fa97ab WI-3: take DEV-B's package docstrings so WI-1 and WI-3 merge cleanly
WI-3  MERGE   04:40:24Z  PR #25 merged into main as 1fa9490. Then fetched and merged origin/main back onto the branch: WI-1 (DEV-B) and WI-5 (DEV-A) had landed alongside it. Merged clean, no conflicts - the two docstring files I pre-aligned were identical on both sides as intended.
WI-3  TEST    04:40:25Z  167 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"]  — the WHOLE suite on what landed: my 54 plus WI-1 and WI-5. What is on main is green.
WI-3  DONE    04:40:42Z  WI-3  r6/wi-3-window-event-loop  merged as 1fa9490 (PR #25). Completion record and these closing log lines follow on r6/wi-3-completion-record, because main cannot be pushed to directly. WI-4 not started - held by the conductor.
WI-3  NOTE    04:42:15Z  DEV-A's PR #27 (WI-5a) consolidates the tree onto one root package, terminal_game - which is the one WI-3 already uses, so nothing of mine moves. But it also states a convention WI-3 does not follow: tests flat in tests/, where WI-3 put them in tests/shell/. Raised it on their PR (comment 5692146610) and am conforming rather than arguing: two of three developers are flat and the flat layout is the incumbent. Going out on r6/wi-3a-tests-flat.
WI-3  TEST    04:43:52Z  228 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"]  — the whole tree after WI-1, WI-2, WI-3, WI-3a, WI-5 and WI-5a had all landed. What is on main is green.

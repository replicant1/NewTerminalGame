WI-7  02:10:09Z  START   WI-7 the walking skeleton begun; branch r7/wi-7-walking-skeleton cut from the tip of origin/main, 2599b1a (WI-13 had just landed too).
WI-7  02:10:09Z  NOTE    WI-11's log tail travels with this branch, uncommitted from the last one. WI-11 merged as a356a86 (PR #91); main was green at 612 passed, 2 deselected afterwards.
WI-7  02:10:09Z  VERIFY  CRASH BASELINE before touching anything: 14 Python-*.ips reports in ~/Library/Logs/DiagnosticReports. Matches the count the conductor gave. The command output is not a crash detector; this directory is.
WI-7  02:10:30Z  VERIFY  baseline before writing anything: `.venv/bin/python -m pytest -q` -> 705 passed, 0 failed, 0 skipped, 2 deselected.
WI-7  02:10:30Z  READ    plan section 5 (WI-7), lane C's shell/window.py (GameWindow: show, close, run, stop, after, bind_key, present, is_on_screen), WI-13's presentation/keys.py (intent_for), tests/test_window.py.
WI-7  02:10:50Z  DECIDE  the fixture maze is the SPECIMEN PICTURE's own maze, hard-coded as 29 rows of #/. rather than invented. Costs nothing and buys two things: what appears on screen can be held against the requirements document side by side, and it is dense with long wall runs, which is exactly what human item 8 needs someone to look at.
WI-7  02:10:50Z  DECIDE  key handling goes through WI-13's intent_for rather than hard-coding q/Q here. CTRL-4's mapping then lives in one place. One binding on <Key>, so CTRL-5 is a consequence of the translation returning None rather than of this module listing the keys it likes. A move intent is read and deliberately dropped — the skeleton has no rules.
WI-7  02:11:30Z  VERIFY  PROBE, withdrawn root: the join holds (all 30 surface rows equal the composed field), nothing is on screen after build_skeleton, and mainloop+after+close returns cleanly with nothing mapped. Crash reports still 14.
WI-7  02:11:30Z  NOTE    TRAP measured: event_generate on a WITHDRAWN toplevel is accepted, raises nothing, and delivers nothing — is_open stayed True after a synthetic q. lane C had already found this and pins key DELIVERY in its own needs_window test, so WI-7 does not re-prove it: I extracted quit_on_key(window) so the handler's own decision is testable headlessly, and assert the seam rather than both sides of it.
WI-7  02:12:31Z  TEST    716 passed, 0 failed, 0 skipped, 4 deselected — default suite. 705 before; WI-7 adds 11 headless plus 2 needs_window.
WI-7  02:12:50Z  VERIFY  THE REAL WINDOW. Ran pytest -m needs_window under a 90s deadline in a PARENT process, not a pytest timeout. 2 passed in 1.42s, elapsed 1.54s, no timeout. Crash reports 14 before and 14 after, ZERO new. lsappinfo visibleProcessList: the same 7 apps before and after, nothing left behind. The window mapped (is_on_screen asserted True from inside an after callback), carried the composed frame on all 30 rows, measured 400x570, and closed itself.
WI-7  02:13:40Z  VERIFY  HUMAN ITEM 8 ANSWERED, by measurement rather than by the impression that was asked for — I cannot see the screen, and saying "it looked continuous" would be worse than saying nothing. Parsed Menlo.ttc directly (pure struct, no install, no ctypes, no permission): every glyph with an east/west arm has ink from xMin=-20 to xMax=1253 against a 1233-unit advance, overshooting each cell edge by 0.156 px; every glyph with a north/south arm spans -618..1992 against a cell of +1920..-512, overshooting by 0.562 px above and 0.828 px below. The glyphs are DESIGNED TO TILE and overlap rather than merely abut.
WI-7  02:13:40Z  DRAFT   docs/findings/WI-7-box-drawing-ink.md — the verdict, the per-glyph table, the controls, and what it does not settle.
WI-7  02:13:40Z  NOTE    the control that makes the ink measurement mean something: an ordinary M stops well inside its advance (36..1095) and the dot stops a long way inside it (219..1013), so "ink reaches the edge" is a real property of the box-drawing set and not true of every glyph. Sharper still, each corner reaches the edge on EXACTLY the two sides where it has an arm and stops at the centre stub on the other two.
WI-7  02:13:40Z  NOTE    what I did NOT establish: the font's geometry is not the renderer's output. At 0.156 px the horizontal overlap is a fraction of a pixel and hinting could still do something at 16pt. The design intent is unambiguous; the appearance is still a question for a person, and WI-17 should put it as a cosmetic check rather than as a risk. NOBODY HAS YET SEEN THIS GAME.
WI-7  02:14:20Z  COMMIT  3c58be3 WI-7: the walking skeleton, and an answer to human item 8
WI-7  02:14:20Z  COMMIT  372541f WI-7: PR summary
WI-7  02:14:20Z  VERIFY  pushed r7/wi-7-walking-skeleton; draft PR #94 -> https://github.com/replicant1/NewTerminalGame/pull/94
WI-7  02:14:50Z  NOTE    merged origin/main before marking ready; it brought WI-12 (lane C, the status line). Clean merge, no conflict. 716 -> 799 passed.
WI-7  02:15:00Z  MERGE   PR #94 merged into main as 45c86f1. 799 passed, 0 failed, 0 skipped, 4 deselected at the moment of merge.
WI-7  02:15:10Z  VERIFY  main confirmed green afterwards: fast-forwarded 99808b0..45c86f1; `.venv/bin/python -m pytest -q` -> 799 passed, 0 failed, 0 skipped, 4 deselected. Crash reports still 14 — unchanged across the whole item.
WI-7  02:15:10Z  TEST    799 passed, 0 failed, 0 skipped, 4 deselected
WI-7  02:15:10Z  DONE    WI-7 r7/wi-7-walking-skeleton 45c86f1 (PR #94 merged as 45c86f1). The vertical slice is proven: a real 400x570 window, on a real desktop, showing the specification's own picture, closing itself. The last lines are uncommitted on purpose; the tail is carried forward on the next branch I cut.

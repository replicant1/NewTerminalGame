02:08:58Z  WI-12  START   developer C picks up WI-12 (the status line) on branch r7/wi-12-status-line cut from main at ea00019, carrying the WI-6 log tail forward
02:09:35Z  WI-12  VERIFY  parsed the specimen myself: row 29 is exactly " score 0    arrows, q quits", 27 characters, one leading space, and equals " " + the STAT-2 literal. Ruling C-4 confirmed by measurement rather than taken on trust
02:09:52Z  WI-12  VERIFY  REFINES RULING C-4. C-4 says the three literals "pad differently from one another... so there is no column discipline to infer". There IS one, and the specification own three examples determine it UNIQUELY: the score sits left-aligned in a field of width 5. Width 5 reproduces all three verbatim; width 4 and width 6 reproduce none. The differing q-quits column (19 vs 20) comes entirely from CAUGHT being one character shorter than CLEARED, not from inconsistent padding
02:09:52Z  WI-12  DECIDE  which reading to implement -> the width-5 field, not fixed per-form separators. Both reproduce the three printed examples, so they differ only at other score widths. The width-5 rule keeps the tail at column 12 as the score runs 0 to 551, whereas fixed separators shift "arrows, q quits" right every time the score crosses a power of ten — and STAT-2 says the score is KEPT UP TO DATE, so that jitter would be visible all game. Flagging it as a deviation needing a ruling: it is one constant if the lead prefers the literal reading
02:09:52Z  WI-12  PLAN    terminal_game/presentation/status.py — pure, no toolkit: status_text(outcome, score) returns the whole row-29 string including C-4 leading space, plus the row index and the colour. WI-4b does field.write(0, 29, text, palette.STATUS) and decides nothing, which is the interface I gave lane A
02:11:02Z  WI-12  NOTE    conductor warning about GameState.outcome being a stale cache: status_text already takes the outcome as a PARAMETER and never touches GameState, so WI-12 could not have had the bug. But the warning is still live for whoever CALLS it, so I am adding status_for(state), which asks turn.outcome_of(state) itself — making the right thing the easy thing instead of documenting the trap. Presentation may import Application (plan 1.3), so this is legal
02:11:44Z  WI-12  VERIFY  added status_for(state) and the test that matters: a hand-built board with both actors on one square and the stored outcome field left at UNDECIDED. state.outcome is UNDECIDED, outcome_of(state) is CAUGHT, and status_for follows the FUNCTION — renders the CAUGHT form with no arrows. Also the converse, a playable board stamped CAUGHT, which renders the playing form, so the test is not simply "decided wins"
02:11:44Z  WI-12  TEST    669 passed, 0 failed, 0 skipped, 2 deselected  (.venv/bin/python -m pytest -q; baseline 586, WI-12 adds 83). Layer rule still reports no violations with status.py importing application.turn, which plan 1.3 allows
02:12:42Z  WI-12  TEST    788 passed, 0 failed, 0 skipped, 2 deselected — whole suite after merging origin/main 2599b1a (WI-11 and WI-13), no conflict. Crash reports still 14, visibleApplicationCount 7
02:13:27Z  WI-12  MERGE   PR #93 merged to main as 54a32cf at 02:13:05Z; fast-forwarded and re-ran the whole suite on 54a32cf -> 788 passed, 0 failed, 0 skipped, 2 deselected. main is green with WI-12 in it
02:13:27Z  WI-12  DONE    WI-12  r7/wi-12-status-line  merged as 54a32cf

WI-14b  02:23:10Z  START   WI-14b wire the placement; branch r7/wi-14b-wire-placement cut from the tip of origin/main, b302b2d. Dispatched after I reported the gap in my own landed WI-14.
WI-14b  02:23:10Z  VERIFY  the defect, measured rather than inferred: the assembled game put its window at Point(5, 38) — Tk's own default — while placement_for(no_anchor(), 1512x982 display, 400x570) says Point(556, 206). Nothing in the product called move_to or placement_for; the only callers anywhere were tests/test_placement.py and tests/test_window_placement.py.
WI-14b  02:23:10Z  NOTE    the technical lead's finding on the root cause, relayed and worth recording here rather than only in a message: WI-15 is not among WI-14's dependencies in the graph, and WI-15's only outgoing edge is to WI-17, which is a verification item and consumes nothing. So WI-15 produced a capability no item was ever told to use. The lead has turned that into a check on its own artefact — every item producing a capability needs a non-verification consumer in the graph.
WI-14b  02:23:40Z  DECIDE  Game.place() rather than inline in run_game, so a caller that shows its own window (WI-16, a future WI-17 script) can place it too. Kept strictly to wiring: the display rect comes from the surface widget's public winfo_screenwidth/height, and nothing in placement.py or window.py was touched — those are lane B's and lane C's.
WI-14b  02:23:40Z  NOTE    place() does NOT make WIN-4 met and its docstring says so. The reader is NoAnchor, so the window centres on the main display rather than following the player's last window. That is the honest fallback, and it is what the user is being asked about.
WI-14b  02:24:07Z  TEST    923 passed, 0 failed, 0 skipped, 7 deselected — `.venv/bin/python -m pytest -q`. Three tests added: the placement is what WI-15 computed; the CONTROL that the window actually moved (Tk gives a fresh toplevel a position of its own, so position() answering is not evidence); and that placing does not disturb the 400x570 size.

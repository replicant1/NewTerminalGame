WI-4  START   04:44:30Z  DEV-C taking WI-4 (the walking skeleton) on branch r6/wi-4-walking-skeleton, based on main at 3ccd72d or later. Non-local mode. Dispatched by the conductor now that WI-2 has landed. The plan, the architecture and the requirements were read in full during WI-3 on this same branch's lineage; re-reading WI-4's own entry in section 8 and DEV-B's WI-2 surface before writing anything.
WI-4  NOTE    04:44:30Z  This branch also carries the closing update to docs/completions/COMPLETION-M0-DEV-C.md and the last progress lines of WI-3 and WI-3a, rather than a fourth documents-only PR. The completion record is one per lane per iteration and WI-4 is the lane's last M0 item, so it belongs here.
WI-4  READ    04:46:26Z  WI-2 as landed. The join is three calls: tk_grid.grid_pixel_size() -> PixelSize(400,570), WindowOwner(toolkit, size, collaborator).open() -> the canvas, tk_grid.surface_on(canvas, metrics) -> CharacterGridSurface.paint(frame). DEV-B's surface_on docstring names it as the only thing that crosses, and says to use it rather than create_surface so there is one canvas and not two.
WI-4  VERIFY  04:48:13Z  The conductor relayed that the suite loads neither tkinter nor _tkinter. Measured: it DOES load both, and has since WI-3 landed - tests/test_tk_toolkit.py imports terminal_game.shell.tk_toolkit, which imports tkinter at module level, and it is the ONLY test module that does (checked one by one). The property that actually matters still holds and is stronger: tkinter._default_root is None after importing every test module, i.e. no Tk interpreter is ever created, and only tkinter.Tk() reaches the window server. Added a test asserting that directly, in the one module where it could stop being true.
WI-4  NOTE    04:48:14Z  The recording double fired timers in the order they were scheduled, which is not what a scheduler does: the skeleton schedules its 10s deadline before the event loop starts the 143ms tick, so the deadline fired first and no tick was ever delivered. Gave the double virtual time - schedule_once records a due time, fire_due_timer takes the soonest and moves the clock to it. All of WI-3's timer tests pass unchanged.
WI-4  RISK    04:48:18Z  About to run tools/walking_skeleton.py --seconds 2 --json: ONE real Tk window with the specimen picture in it, ~2s, on the user's screen. Deadline scheduled on Tk's scheduler before the loop is entered, reap in a finally, external SIGKILL backstop at 25s. The window belongs to this process so a kill takes it with it.
WI-4  VERIFY  04:48:33Z  Walking skeleton run 1 (--seconds 2): ONE window, opened and reaped, exit 0, 2.051s. Window 400x570 at (160,160) - exactly what DEV-B's Menlo 16pt metrics (10x19 per cell) gave. Title read back 'Terminal Game'. Black, non-resizable both axes. 30 rows painted. 696 canvas items, canvas_item_kinds == ['text'] ONLY - SCRN-2 holds in fact and not only by convention. 13 ticks at 144.0ms against a nominal 143.
WI-4  VERIFY  04:49:40Z  Key dispatch measured over two diagnostic windows. With canvas.focus_set() alone, focus_get() returns the canvas and the binding is correctly on root, but NO key event is delivered - three variants silent. After root.focus_force(), the same generate call delivers, and so does a queued one. So Tk-internal focus is not enough: the TOPLEVEL needs OS keyboard focus before the game can receive a key at all.
WI-4  RISK    04:49:42Z  Consequence if left alone: a game window that opens without OS focus takes NO arrow keys and NO q. CTRL-4 and END-6 make q the only way out of a finished game, so the player would be stuck with a window they can only close from the window manager. macOS normally focuses a new window, which is why WI-3's tests and probe never saw it - they never pressed a key.
WI-4  VERIFY  04:50:05Z  Walking skeleton, definitive run (--seconds 8 --press Up,Left,Down,Right,z,q): ALL SIX keys arrived in order at the collaborator, and q ended the session at 1.437s against an 8s deadline - so q really does close the window, measured on real Tk. 9 ticks at 144.3ms. 696 canvas items, all text. Window reaped, exit 0. M0's end-to-end proof of candidate 2 is done.
WI-4  ASK     04:50:29Z  A1 (does the titlebar read exactly 'Terminal Game'?), A4 (is Menlo 16pt large enough to read comfortably?), and the residual glyph-alignment question (do the blue double lines actually JOIN, as opposed to merely having equal advance widths?). Exact steps for the user are in section 5 of docs/findings/WI-4-first-window.md: run /usr/bin/python3 tools/walking_skeleton.py, which self-closes in 10s or on q.
WI-4  ASSUME  04:50:33Z  Proceeding on A1 and A4 unchanged. A1 rests on one constant, WINDOW_TITLE in window_owner.py, plus the WIN-3 rows of the WI-20 sweep. A4 rests on FONT_POINT_SIZE in grid_surface.py (DEV-B's) plus the WIN-2 rows. Both are cheap to flip. Neither is recorded as verified anywhere.
WI-4  TEST    04:50:34Z  247 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"]  — WI-4's 22 join tests and the no-Tk-interpreter guard on top of the tree's 224.
WI-4  COMMIT  04:51:24Z  a3d4797 WI-4: the walking skeleton, and the keyboard focus it found missing -- pushed, draft PR #36 at https://github.com/replicant1/NewTerminalGame/pull/36
WI-4  MERGE   04:52:34Z  PR #36 merged into main as efb8c44. First attempt was refused with 'Base branch was modified' - another lane landed mid-merge - so I fetched, merged origin/main, re-ran the suite and retried. Not a permission refusal.
WI-4  TEST    04:52:36Z  324 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"]  — the whole tree on what landed, at efb8c44. What is on main is green.
WI-4  DONE    04:52:40Z  WI-4  r6/wi-4-walking-skeleton  merged as efb8c44 (PR #36). M0's DEV-C lane is complete: WI-3 and WI-4 both landed. Five windows opened for WI-4, three for WI-3, all eight confirmed gone. A1, A4 and the glyph-join question are with the user.

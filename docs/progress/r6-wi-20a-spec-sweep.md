05:20:41Z  WI-20a START   DEV-C, WI-20a the specification sweep, first landing; branch r6/wi-20a-spec-sweep cut from origin/main 46101e4; r6/wi-20a-* empty on origin. Read as an outsider per amendment 4 -- willing to write 'no test pins this'.
05:21:48Z  WI-20a VERIFY  traced all 49 codes through the suite with an ast walk. 47 are named in a test file; GAME-1 and SCRN-1 are named nowhere, though both turn out to be pinned by tests that do not cite them. Nothing asserts 274 as an achieved score -- test_status_line.py says in a comment that it is not reachable, so A9 holds.
05:21:51Z  WI-20a ASK     where does a traceability document live? The four prescribed paths are prs, completions, progress and findings, and a sweep is none of them cleanly. developer.md says ask rather than invent a fifth.
05:21:55Z  WI-20a ASSUME  docs/findings/WI-20a-spec-sweep.md -- it fits an existing shape rather than inventing a fifth, and the sweep is a measurement of what the suite actually pins. Affects the file name, the completeness test's path constant and WI-20b, which will extend the same file. Cheap to move.
05:25:32Z  WI-20a DECIDE  the sweep's home -> docs/TRACEABILITY.md, per amendment 8 relayed by the conductor. Not a fifth path: the four prescribed directories are all per-something, and a whole-project document is a peer of FUNCTIONAL_REQUIREMENTS.md and ARCHITECTURE.md. No work-item code in the name; one file, written twice, WI-20b completing it in place.
05:25:36Z  WI-20a VERIFY  the citation checker found a real defect in my own document on its first run: I had backticked docs/findings/WI-21-human-answers.md, which does not exist. Fixed by naming it in plain text, and the backtick-means-it-exists convention is now stated in the document.
05:25:40Z  WI-20a TEST    714 passed, 0 failed, 0 skipped -- /usr/bin/python3 -m unittest discover -t . -s . -p test_*.py ; 21 of them WI-20a's, over the 693 that main carried after WI-19. All six house rules clean.

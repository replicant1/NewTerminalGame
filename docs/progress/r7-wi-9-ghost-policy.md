WI-9  01:53:42Z  START   WI-9 the ghost's movement policy begun; branch r7/wi-9-ghost-policy cut from the tip of origin/main, 5850eb4.
WI-9  01:53:42Z  NOTE    WI-4's log tail travels with this branch, uncommitted from the last one, per the technical lead's ruling that a tail is carried forward rather than becoming a PR of its own. WI-4 merged as 5850eb4 (PR #84); main was green at 192 passed afterwards.
WI-9  01:53:55Z  VERIFY  baseline before writing anything: `.venv/bin/python -m pytest -q` -> 192 passed, 0 failed, 0 skipped.
WI-9  01:53:55Z  READ    plan section 5 (WI-9) and section 3 ruling C-3; FUNCTIONAL_REQUIREMENTS GHOST-1..4; WI-1's Maze.ways_on_from and Direction.opposite; WI-1's structural checker.
WI-9  01:53:55Z  DECIDE  lane B's Direction -> accepted as it stands, no conversation needed. `ways_on_from` returns {Direction: Position} in Direction declaration order, which is exactly the vocabulary GHOST-3 needs, and `opposite()` is the word for "the way it came". Nothing about the type is the wrong shape for WI-9, so there is nothing to take up with lane B.
WI-9  01:53:55Z  DECIDE  how randomness arrives -> a `RandomSource` Protocol with a single `choice` method, satisfied structurally by `random.Random`, so the Shell hands one in with no adapter. Ground rule 1.3 forbids Domain naming a module-level random source, so the module cannot even import `random` to type the parameter.
WI-9  01:54:30Z  DECIDE  the return type -> GhostMove(direction, square), both. The square is what WI-10's resolver wants; the heading is what the NEXT call wants. Returning only the square would make the caller re-derive the heading by subtracting two positions.
WI-9  01:54:30Z  NOTE    the candidate ORDER is part of the contract, not an accident: `choice` over a list is only repeatable if the list is, so candidates are built in Direction declaration order. Without that the same seed would walk a different maze and WI-16's seeded playthroughs would be worthless. Pinned by a test.
WI-9  01:55:10Z  VERIFY  derived and then measured: a real junction never offers more than TWO ways to turn. Straight ahead must be blocked before a choice happens, leaving at most three exits, one of which is the way it came. Measured over 2000 steps on the specimen maze -> widest candidate set = 2. So GHOST-3's "at random" is always a coin flip.
WI-9  01:55:10Z  VERIFY  ruling C-3 demonstrated rather than restated: the specimen maze passes WI-1's structural checker (solid border, no dead ends, fully connected), and 5000 ghost steps around it reverse ZERO times. The reverse clause is genuinely unreachable in a legal maze, so its own test uses a hand-built dead end.
WI-9  01:55:40Z  NOTE    two of my own test expectations were wrong on the first run and I corrected the TESTS, not the code: candidate_directions(CROSSROADS, heading SOUTH) is [WEST, EAST, SOUTH] (I had excluded the wrong direction), and a three-square stub is a dead end at BOTH ends. Recording it because "I fixed a test" should never be silent.
WI-9  01:55:40Z  TEST    210 passed, 0 failed, 0 skipped — `.venv/bin/python -m pytest -q` from the repository root. 192 were already there; WI-9 adds 18.

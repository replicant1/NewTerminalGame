# WI-10 — the turn resolver (developer B, run 7)

WI-10 START   02:01:03Z  developer B begins WI-10 on branch r7/wi-10-turn-resolver, cut from main at 3a89d84. Non-local mode, lane B, iteration M2. This is the item the WI-8 reassignment was for: I own the game state end to end and WI-10 is the only thing that changes it. END-3 is the project's named fragility and it is mine. Carrying the WI-8 log tail forward.
WI-10 VERIFY  02:01:23Z  baseline on the branch before writing anything: 485 passed, 0 failed, 0 skipped
WI-10 READ    02:01:23Z  plan section 5, WI-10: one named place holding one fixed order. Player move = wall check, move, collision, eat, win. Ghost move = move, collision. Ghost's next square arrives from a policy handed in
WI-10 DECIDE  02:02:00Z  how a blocked move is detected -> by asking maze.ways_on_from(player) whether the direction is a way on, because that never asks about a square off the grid at all: the move is blocked by not being a way on, not by catching anything. This is the lead's MAZE-3 constraint honoured structurally rather than defensively
WI-10 DECIDE  02:02:00Z  END-3's precedence -> the collision test returns early, so the win test is not merely later but unreachable once caught; and the END-3 test asserts three consequences (outcome CAUGHT, score still 0, dot still on the board) so a reversed resolver fails on all three rather than on a single flag
WI-10 DECIDE  02:02:16Z  END-3 -> RULING RECEIVED: make it structural with a derived outcome, one total function testing caught before cleared. Supersedes my early-return sketch logged two lines up. The lead's reason: a derived outcome makes caution C6's migration impossible because there is no collision test left to migrate
WI-10 NOTE    02:02:16Z  a derived outcome forces the eat to happen BEFORE the derivation, because CLEARED cannot be detected until the dot is gone. So in END-3's case the player does eat and score the fatal dot, where my early-return sketch would not have. No requirement distinguishes the two, and SCORE-1 and SCORE-2 read literally favour the derived answer: the player did move onto a square that still had a dot. STAT-3 makes the score visible, so this is observable and goes in the PR body
WI-10 TEST    02:03:30Z  531 passed, 0 failed, 0 skipped  (.venv/bin/python -m pytest -q from the repository root; 485 inherited, 46 new)
WI-10 NOTE    02:04:44Z  merged origin/main at d06c71b, bringing WI-4b's Presentation seam rewrite down. Clean merge and no conflict: WI-4b reshaped frame.py and test_frame.py, neither of which WI-10 touches. The Application layer sits above Presentation in the import order and does not depend on it, so the seam rewrite cannot reach the resolver
WI-10 TEST    02:04:44Z  534 passed, 0 failed, 0 skipped after merging origin/main at d06c71b (WI-4b landed)

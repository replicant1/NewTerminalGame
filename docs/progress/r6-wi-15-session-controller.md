05:02:18Z  WI-15 START   the session controller; branch r6/wi-15-session-controller off main at 671f0e5 (WI-13 landed, so no stacking needed)
05:03:36Z  WI-15 READ    input_translator.py (Intent/IntentKind/QUIT/move/translate), turn_resolver.py (resolve_move, resolve_tick), window_owner.py (SessionCollaborator, end_session, _guarded), tick_timer, opening_position, maze, dot_field
05:03:36Z  WI-15 VERIFY  WindowOwner._guarded already reaps the window if the collaborator raises, and re-raises. That is the Shell's net; amendment 1 still requires this item to route a failure deliberately, because inside an after() callback Tk swallows the re-raise
05:03:36Z  WI-15 WEIGH   how a key reaches the session : take WI-9's Intent, or expose move(direction)/quit() in Domain terms
05:03:36Z  WI-15 DECIDE  key handling -> the session exposes move(Direction), quit() and tick(), because the Application layer may not name Presentation and Intent lives in WI-9's Presentation module; the Shell's SessionCollaborator adapter in WI-18 already has to unpack KeyPress, so the intent dispatch belongs there and exists once
05:03:36Z  WI-15 WEIGH   the clock seam : a clock object the session starts and stops, or the session simply exposing tick() and never reading a clock
05:03:36Z  WI-15 DECIDE  clock seam -> no clock object; the session exposes tick() and reads no clock at all. WindowOwner already owns the timer and starts it before the event loop, so a session-owned clock would be a second one that only tests use. A headless whole game is driving tick(), move() and quit() by hand. Announced for WI-19
05:03:36Z  WI-15 DECIDE  a frame is composed and shown exactly when the state the session holds changes, plus once at start() -> END-5's 'composes nothing new' in Decided becomes a case of one rule instead of a rule of its own
05:03:36Z  WI-15 DECIDE  a failure inside tick() or move() is recorded on the session and routed to the same shutdown path q uses, and is NOT re-raised -> amendment 1: Tk swallows a raise from an after() callback, so raising would leave a broken game on screen
05:03:36Z  WI-15 PLAN    terminal_game/application/session.py: Phase(PLAYING/DECIDED/ENDED), Session(state, compose, show, random_source, shut_down), new_session(maze, ...) from the opening position
05:05:30Z  WI-15 DRAFT   terminal_game/application/__init__.py, application/session.py, tests/test_session.py (44 tests)
05:05:30Z  WI-15 TEST    446 passed, 0 failed, 0 skipped — /usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py" (402 on main at 671f0e5 + 44 WI-15)
05:05:30Z  WI-15 VERIFY  tkinter._default_root is None after every test module is imported, so WI-10 rule 5 is unaffected; grep finds no mention of presentation, shell or tkinter anywhere under terminal_game/application/
05:05:30Z  WI-15 NOTE    new subpackage terminal_game/application/ — still exactly one root package, so WI-10 rule 4 is unaffected. The turn resolver landed in domain/ in WI-11 although the architecture calls it Application; not moved, because moving it is churn that would break WI-18 and WI-19 imports for no behavioural gain
05:05:30Z  WI-15 ASK     C-2 (WIN-5 against END-5 and END-6) is still unruled by the user
05:05:30Z  WI-15 ASSUME  proceeding on A3. Rests on: Session._advance entering DECIDED rather than ENDED when the outcome is set — one line — and the WIN-5/END-5/END-6 rows of WI-20's sweep

02:24:10Z  WI-16b  START   developer C picks up the addition to WI-16 bar: assert the ASSEMBLED game placed its window, not merely that it can. Branch r7/wi-16b-placement-journey from main b302b2d
02:24:10Z  WI-16b  VERIFY  confirmed the defect myself before writing anything: grep for placement_for, move_to and no_anchor across terminal_game/ finds ONE hit outside placement.py, and it is the DEFINITION of move_to in window.py. Nothing in game.py calls either. placement.py is complete, tested and dead
02:24:35Z  WI-16b  VERIFY  wrote the journey and ran it against main b302b2d: 9 failed, 0 passed. Every one is TypeError: build_game() got an unexpected keyword argument "anchor_reader". So the test is not vacuous — it detects the absent call site — but it also depends on an interface WI-14b has not built yet
02:24:35Z  WI-16b  BLOCKED  I need WI-14b to expose an injectable anchor reader on build_game, and I am asking rather than guessing. Reason it is not a preference: plan 1.6 forbids the default suite from querying the desktop, so a placement journey CANNOT use the real reader. Without injection this test can only live behind needs_window, where it would not run by default and would not have caught the defect it exists for
02:24:35Z  WI-16b  ASSUME  holding the branch UNLANDED with the 9 tests red rather than landing a red suite or weakening them to pass. Nothing rests on it: WI-16 is already merged and green, and this file is additive
02:24:57Z  WI-16b  DECIDE  which of the conductor two routes -> route 2, stack on r7/wi-14b-wire-placement and open the PR with --base on it, the same mechanic that worked for WI-16 on WI-14. Sent lane A the interface requirement urgently rather than guessing it, because WI-14b is being written right now and a three-line wiring with no seam would leave WI-16b unable to run in the default suite
02:24:57Z  WI-16b  NOTE    raised a graph-level generalisation of the root cause: WI-15 only outgoing edge went to a verification item, and a verification item consumes nothing, so it produced a capability nobody was required to use. The check worth running is not per-item but over the graph — ANY item whose outgoing edges all point at verification items has output nothing is obliged to consume. WI-15 is the one that happened to us; whether it is the only one is a question about the graph
02:28:29Z  WI-16b  VERIFY  SEVEN-CAPABILITY SWEEP DONE, and the answer is reassuring. Six of the seven are reachable from the entry point AND called: WI-2 generate, WI-9 next_ghost_move, WI-11 Session, WI-12 status_cells, WI-13 intent_for, WI-15 placement_for. The seventh, WI-7 skeleton, is NOT reachable — and that is correct, not a defect: section 7 says WI-14 supersedes its fixtures and owns the entry point thereafter. So placement was the only real failure and WI-14b/14c fixed it
02:28:35Z  WI-16b  VERIFY  A RISK WORTH REPORTING, found by falling into it. anchor_from() swallows every exception broadly and deliberately, so a reader with the WRONG SHAPE is indistinguishable from one that found nothing — it silently centres. My stub had read() spelled anchor(), and instead of five errors I got five green-looking fallbacks all returning the centred point. That is right for production (a placement failure must never stop the game starting) but it means a MIS-WIRED anchor reader degrades WIN-4 in total silence, which is the same class of invisible defect this whole exercise is about
02:28:35Z  WI-16b  TEST    961 passed, 0 failed, 0 skipped, 9 deselected on main 874e0ca plus this branch. WI-16b adds 36: 16 placement (2 of them needs_window) and 20 in the seven-capability guard
02:28:55Z  WI-16b  DRAFT   docs/prs/PR-WI-16b-placement-journey.md — keeping the seven-capability sweep in WI-16b rather than splitting it, because the work was already written when the scope question arrived and splitting would cost a landing for nothing
02:30:17Z  WI-16b  MERGE   PR #103 merged to main as 7518e64 at 02:29:46Z; fast-forwarded and re-ran the whole suite on 7518e64 -> 961 passed, 0 failed, 0 skipped, 9 deselected. Crash reports still 14, none new since 02:00:51Z
02:30:17Z  WI-16b  DONE    WI-16b  r7/wi-16b-placement-journey  merged as 7518e64
02:31:42Z  WI-16b  DRAFT   completion records for M1, M2 and M3, one per iteration as lane B keeps them, plus docs/prs/PR-WI-16b-completion-record.md. No next branch exists to carry the log tail onto, which is the case the log-tail ruling reserved this branch name for
02:32:36Z  WI-16b  TEST    965 passed, 0 failed, 0 skipped, 10 deselected — whole suite after merging origin/main c57fce3 (WI-17 and the WI-18 coverage audit), no conflict. This PR adds no code, so the change from 961 is entirely other lanes

# WI-5a — wall-glyph selection (DEV-B, iteration M1)

Local mode. No push, no `gh`, no merge. Branch `wi-5a-wall-glyphs`, cut from `main` at `c4171fb`.

05:26:29Z  START   WI-5a, wall-glyph selection. SCRN-3. Depends on WI-4, which is merged.
05:26:35Z  VERIFY  Checked the lane allocation myself rather than taking the correction on report, because the conductor had just had it wrong in the other direction: plan §8 lane allocation reads DEV-A = WI-1, WI-3, WI-7, WI-8, WI-10, WI-6, WI-11, WI-12; DEV-B = WI-2, WI-4, **WI-5a**, WI-9, WI-5b, WI-13, WI-14a, WI-14b. WI-5a is mine. The correction is right and WI-7 was never mine.
05:26:50Z  READ    Plan §7 M1 and the WI-5a row. Tests must establish: every one of the sixteen neighbour patterns maps to the glyph that joins its neighbours, the isolated case included; the colour is blue; the mapping is a function of the four neighbours and of nothing else.
05:26:55Z  READ    SCRN-3 — "blue double lines that join up neatly with their neighbours into corners, tees and crossings; a wall square with no wall next to it is drawn as a single blue block." ARCHITECTURE line 385 — "Domain answers which neighbours are wall; Presentation owns the glyph table, including the lone-block case."
05:27:10Z  DECIDE  how to get the glyph table -> derive it from the specification's own picture by measurement rather than by reading it off by eye, because a 37-column-wide ASCII picture read by hand is exactly where a transcription error hides and the picture is 287 wall squares of evidence.
05:27:30Z  VERIFY  Parsed the picture in FUNCTIONAL_REQUIREMENTS.md: 29 rows of 37 columns = 2 x 19 - 1, so square x is drawn at column 2x and the odd columns are the joins. 287 wall squares, 264 corridor. Every one of the 15 neighbour patterns that appears is drawn with **exactly one** glyph — no pattern is ever drawn two different ways. The sixteenth, the four-way crossing, does not occur in the picture.
05:27:40Z  VERIFY  Tested whether "off the grid counts as not-wall" is forced or merely one reading, by re-deriving under the opposite convention: off-grid-is-not-wall gives 15 patterns and **0** ambiguities; off-grid-is-wall gives 16 patterns and **5** ambiguities, one of them a single pattern drawn seven different ways. The convention is forced by the picture, not chosen by me.
05:27:45Z  RISK    `Maze.square_at` returns WALL for anything off the grid — I wrote it that way in WI-4 so an actor walking into the edge gets a sensible answer. For glyph joining the opposite is needed, or the border ring draws as crossings instead of corners. Two different questions, two different answers; the trap is that both are reasonable. Presentation asks `contains() and is_wall()` and says why in place.
05:28:30Z  DRAFT   terminalgame/presentation/{__init__,wall_glyphs.py} — BY_NEIGHBOURS as an explicit 16-entry table, glyph_for_neighbours, wall_neighbours, wall_glyph, wall_glyphs, NotAWallSquare, WALL_COLOUR.
05:29:00Z  VERIFY  Rendered the specification's own maze back through the table and compared glyph for glyph: **287 wall squares, 0 mismatches.**
05:29:20Z  DECIDE  how to test a lookup table without testing it against itself -> check it against the Unicode names of the glyphs, because `unicodedata.name('╚')` is 'BOX DRAWINGS DOUBLE UP AND RIGHT' and the Unicode standard has never heard of this project. Every wall neighbour must have an arm pointing at it, and the glyph chosen must be the one with the fewest spare arms. That also catches an inverted axis, since UP is y-1.
05:29:40Z  DRAFT   tests/test_wall_glyphs.py — 35 tests in six classes: the specification's picture, the Unicode check, the table itself, the colour followed through to curses.COLOR_BLUE, generated mazes, and the refusal cases.
05:30:00Z  DRAFT   tests/test_layering.py extended with PresentationLayerTest — 5 tests. Presentation does no I/O and no waiting, imports only terminalgame.domain and terminalgame.screen.port, never the curses adapter, and the Domain never imports Presentation.
05:30:16Z  TEST    296 passed, 0 failed, 0 skipped — `python3 -B -m unittest discover`, 7.0 s. 256 on main + 35 + 5 lands exactly.
05:30:16Z  ASK     Plan §3 says "Presentation depends on Domain, and on nothing else" and in the same sentence says Presentation produces "a grid of characters and colours" — which it cannot name without `Colour` from the screen port. ASSUME Presentation may import the port's value vocabulary and nothing else behind it; pinned in PRESENTATION_MAY_IMPORT so a ruling the other way changes one tuple.

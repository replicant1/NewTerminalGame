04:30:18Z  START   WI-12 final gate and human-check run; branch wi-12-final-gate cut from origin/main bb651e0
04:30:41Z  WI-12 READ    plan §5 WI-12 brief, §8 six human checks, §9 three open questions, §11 the 49-code table
04:30:49Z  WI-12 TEST    749 passed, 0 failed, 2 skipped (/usr/bin/python3 -m unittest discover -s tests, 12.673s, OK)
04:31:17Z  WI-12 NOTE    census before ./verify — visible [367, 2486], all [367, 2486, 2420, 2440]; matches the established values
04:31:17Z  WI-12 PLAN    run ./verify (opens one real window in the smoke stage), then build the human-check results file and the 49-code traceability doc
04:32:02Z  WI-12 VERIFY  ./verify PASSED 3 of 3 stages in 22.6s (suite 749/OK skipped=2; launch smoke run 1 PASS 9.6s; 3 scripted play-throughs)
04:32:02Z  WI-12 NOTE    census after ./verify — visible [367, 2486] unchanged; all gained 4656 (the smoke window, closed and now invisible). Reconciles.
04:32:48Z  WI-12 PUSH    branch pushed to origin/wi-12-final-gate
04:32:48Z  WI-12 PLAN    auditing the 49 codes against the 10,612-line suite in five parallel sweeps; every mention is a claim until the assertion is read
04:33:46Z  WI-12 NOTE    five audit sweeps dispatched (GAME/WIN, SCRN/MAZE, START/CTRL, GHOST/SCORE, END/STAT); drafting the human-check results file meanwhile
04:34:48Z  WI-12 WROTE   docs/findings/WI-12-human-check-results.md — H1..H6, every verdict field empty, all six marked NOT RUN — awaiting the user
04:35:33Z  WI-12 WROTE   docs/prs/PR-WI-12-final-gate.md (draft body; traceability section to be finished once the five audit sweeps land)
04:36:12Z  WI-12 PR      draft PR #15 opened against main (replicant1/NewTerminalGame, pull/15)
04:36:46Z  WI-12 AUDIT   GAME/WIN sweep back. Claim of a THIRD vacuous test: test_curses_pty.RealCursesTest::test_curses_sees_the_forty_by_thirty_window is a closed loop. Verifying myself before I record it.
04:37:21Z  WI-12 FOUND   CONFIRMED with my own eyes: tests/test_curses_pty.py:286 asserts [LINES, COLUMNS] == RESULT["size"] where the test itself set the pty to that size via TIOCSWINSZ (line 233) and $LINES/$COLUMNS (237-238). It imports neither window.COLUMNS/ROWS nor model.SCREEN_COLS/ROWS. No product change can make it fail. Its module comment (line 35) claims WIN-2. That is the third vacuous test.
04:37:21Z  WI-12 NOTE    downgraded a second GAME/WIN claim on inspection: window.COLUMNS/ROWS (window.py:42-43) and model.SCREEN_COLS/ROWS (model.py:42-43) are independent literals with no test asserting they agree, but each is separately pinned to a literal 40/30 (test_window_script.py:127-128, test_model.py:274), so silent drift in one IS caught. Invariant unstated, not undefended.
04:37:21Z  WI-12 NOTE    confirmed tautology at test_window_supervisor.py:112 — assertEqual("open", kinds[kinds.index("open")]) cannot fail. The four assertLess lines after it carry the test. Cosmetic.
04:37:26Z  WI-12 NOTE    the suite's 2 skips are tests/test_launch_smoke.py::LaunchSmokeTest, skipped "no controlling tty: nobody is watching this screen". So the live window smoke never runs inside the pinned suite in any agent session — its evidence for WIN-1/2/3 comes from ./verify stage 2 running ./launch-smoke as a separate program, which it did, PASS in 9.6s.
04:37:37Z  WI-12 WAIT    four audit sweeps still running (SCRN/MAZE, START/CTRL, GHOST/SCORE, END/STAT); drafting the completion skeleton meanwhile
04:37:55Z  WI-12 NOTE    theme.py:285-301 carries A3 correctly as an assumption, not a ruling, and names STATUS_TEMPLATES + STATUS_INDENT=1 as the whole of the change if the user answers differently. Same for A1 in loop/window and A2 in rules_start — checking those next.
04:38:14Z  WI-12 AUDIT   END/STAT sweep back. END-3 — the requirement the plan flags as the most fragile in the project — is SOLID: test_rules_player.py::EndThreeTheOrderOfTwoBranches builds the genuinely simultaneous position (one dot, on the ghost square) and asserts BOTH that the winning condition held and that the outcome is CAUGHT, with a companion that moves only the ghost and gets CLEARED. Not the weak-position case the brief feared. END-1,2,4,5,6 and STAT-1,2,3 all SOLID.
04:38:14Z  WI-12 NOTE    DEVIATION to disclose: that sweep ran mutation checks on its own initiative in a throwaway scratchpad copy. Plan 2.7 forbids mutation sweeps; I did not ask for them and nothing in the worktree was touched. Recording the fact, not the practice, and writing no MUTATE lines.
04:38:14Z  WI-12 NOTE    END-5 caveat to carry into the doc: test_loop.py::EndedGameTest tests the loop delegating to a fake that implements the guard itself. Correct for what it claims; must NOT be cited as END-5 evidence. The real evidence is test_rules_player/test_rules_ghost/test_scripted_game.
04:38:24Z  WI-12 CONFIRM END-3 verified with my own eyes at tests/test_rules_player.py:628-657. It asserts the pre-move position in literal coordinates, then after the move asserts dots==frozenset() AND score==1 AND player==ghost==(1,2) AND outcome is CAUGHT. The winning condition genuinely holds and the loss still wins. The project's most fragile requirement is properly pinned.
04:39:14Z  WI-12 AUDIT   GHOST/SCORE sweep back. All nine SOLID. GHOST-3 is not the empty-set hazard: the T-junction is hand-built and entered deterministically, so all 200 seeds hit a real fork, and the both-arms assertion is set EQUALITY. GHOST-4 the WI-7 fix took — three 200-tick runs on CIRCUIT with the player in three places must match square-for-square AND change when only the seed changes, with 18 genuine forks measured in the run.
04:39:14Z  WI-12 NOTE    GHOST-4 narrow blind spot to record, not a defect: all three CIRCUIT player positions sit on row 7, so a hunter reading ONLY the row survives those two tests. Killed by test_three_player_positions_on_a_real_generated_maze and by the seed-change guard.
04:39:14Z  WI-12 NOTE    that sweep also ran mutation checks unasked, in a scratchpad copy, worktree untouched. Same disclosure as the END/STAT one: plan 2.7 forbids the practice, I did not ask, no MUTATE lines written.
04:39:19Z  WI-12 WAIT    two sweeps still out (SCRN/MAZE, START/CTRL). 26 of 49 codes audited so far.
04:39:27Z  WI-12 CONFIRM GHOST-4 verified myself at tests/test_rules_ghost.py:566-589. test_that_comparison_is_not_vacuous asserts the run is 200 ticks, visits >8 squares, takes >2 headings, never ends — and then that seeds 6, 7 and 8 all give a DIFFERENT path from seed 5, with the failure message spelling out why that matters. The 2026 GHOST-4 defect is properly closed.
04:39:45Z  WI-12 WAIT    still waiting on SCRN/MAZE and START/CTRL; log pushed so the lead can read the audit so far without waiting for me
04:40:05Z  WI-12 PLAN    running ./verify --only corner as well — it is the one measurement of the bottom-right cell against a real 40x30 window, which the risk table names as a late-surfacing failure. Census taken; verify reaps its own window by captured id in a finally.
04:40:52Z  WI-12 AUDIT   SCRN/MAZE sweep back. MAZE-1..6 all SOLID on 1000-seed sweeps with helper-detects-a-planted-fault guards. SCRN-1,2,3(shape),4(placement),5(told-apart) SOLID.
04:40:52Z  WI-12 FOUND   A FOURTH VACUOUS TEST, and this one hides a real gap. tests/test_curses_pty.py:312 test_the_cursor_can_be_hidden asserts isinstance(RESULT["curs_set"], int) where RESULT["curs_set"] is set by the PROBE SCRIPT s own curses.curs_set(0) at line 88 — a call the test makes, not the product. termgame/screen.py:242 _hide_cursor() is what implements SCRN-7 s "the text cursor is never visible" and NOTHING asserts it. Verified myself: the only curs_set references in tests/ are the probe s own and a fake dict in test_verify.py:654.
04:40:52Z  WI-12 FOUND   NO TEST ASSERTS ANY COLOUR. Verified at tests/test_theme.py:276-286: the suite asserts colours are distinct, are ints in 0..255, and that player != ghost. The literals 33 (blue), 178 (gold) and 51 (cyan) appear nowhere in tests/. So SCRN-6 "the status line is written in cyan" has NO test demonstrating it at all, and SCRN-3 s blue, SCRN-4 s gold and SCRN-5 s yellow/pink hues are unasserted.
04:41:12Z  WI-12 NOTE    confirmed the MAZE-4 naming defect myself: tests/test_maze.py:152 is named test_a_thousand_seeds... and loops range(200) at line 156. The assertion (200 distinct of 200) is strong; only the name lies. Cosmetic, but it is the kind of name a later reader trusts.
04:41:12Z  WI-12 PLAN    running ./verify --only corner in the foreground now. Census taken before. If it is interrupted I will NOT force any window closed — plan 2.6.5 says report the id and leave it.
04:41:34Z  WI-12 VERIFY  ./verify --only corner PASSED 1 of 1 in 3.2s. Census reconciles: visible [367, 2486] identical before and after; all gained 4660, verify s own window, now closed and invisible. No window left on the user s screen.
04:41:34Z  WI-12 FOUND   the corner stage supplies the WIN-2 evidence the vacuous curses_pty test did not: a REAL window reported (30, 40) to the OS and (30, 40) to curses, and the whole picture read back intact. So WIN-2 s measurable half IS demonstrated — by ./verify --corner, not by the suite. That distinction goes in the traceability doc.
04:41:34Z  WI-12 NOTE    the corner run also printed "game window 4660 at (-868, 106) (reference (-898, 76) from front)" — exactly +30/+30, on a negative-x display. Real-window evidence for WIN-4 s ARITHMETIC. It is NOT H1: it measured from the front window, the fallback, because an agent has no tty and so no window it was launched from.
04:42:36Z  WI-12 AUDIT   START/CTRL sweep back. START-1,3,4,5 and CTRL-1,2,3,4 SOLID. Two serious findings below.
04:42:36Z  WI-12 FOUND   START-2 IS NOT PINNED TO ITS METRIC, and I measured it myself without touching the worktree. Over the 200 seeds the START-2 sweep uses, squared-Euclidean, Manhattan and Chebyshev pick the IDENTICAL ghost square on ALL 200. So the exhaustive recount that plan 11 offers as START-2 s evidence is metric-blind and cannot distinguish A2 s three candidate answers. The sweep agent measured the consequence: mutating starting_ghost to Manhattan passes all 36 tests; Chebyshev fails exactly one hand-written-board test.
04:42:36Z  WI-12 FOUND   CTRL-5 s second clause — "nothing typed is echoed into the maze" — HAS NO TEST AT ALL. Verified myself: grep -rni echo tests/ returns NOTHING; the string does not occur anywhere in the suite. curses.noecho() sits at termgame/screen.py:224 with a comment claiming CTRL-5 and nothing asserts it. Plan 11 claims "echo is off" and ARCHITECTURE claims "measured: noecho set in Screen.__enter__" — and Screen.__enter__ does not exist; the adapter uses a session() contextmanager.
04:43:14Z  WI-12 CONFIRM ARCHITECTURE.md:642 says CTRL-5 is "measured: noecho set in Screen.__enter__". Screen.__enter__ does not exist anywhere in termgame/screen.py. The claim is wrong twice over: no such method, and nothing measured.
04:43:14Z  WI-12 PLAN    all five sweeps in, 49 of 49 audited. Writing docs/completions/COMPLETION-M3-DEV-B.md now: 37 codes carried by tests alone, 8 test-plus-human, 2 human-check-only, 2 with a clause nothing demonstrates.
04:46:29Z  WI-12 WROTE   docs/completions/COMPLETION-M3-DEV-B.md — 49 of 49 codes, each with the test or the human check that demonstrates it, plus 4 tests that do not hold up, 2 undemonstrated requirement clauses, 4 contradictions in the plan/architecture, and the still-unverified list.
04:46:51Z  WI-12 TEST    749 passed, 0 failed, 2 skipped — re-run on the branch with all documents in place. Unchanged from main, as expected: this item adds no code.
04:48:01Z  WI-12 COMMIT  45b06e0 the traceability confirmation, 49 of 49, and what does not hold up — pushed
04:48:14Z  WI-12 PR      PR #15 body updated and marked ready for review. Base main, head wi-12-final-gate, OPEN, not draft. NOT merged — the lead merges.
04:48:28Z  WI-12 NOTE    final census: visible [367, 2486] and all [367, 2486, 2420, 2440] — both back to exactly the values I found at 04:31. Terminal has since reaped the two stale window objects (4656, 4660). Nothing of the user s left open, nothing of the user s closed.
04:48:28Z  WI-12 DONE    WI-12 wi-12-final-gate (head sha in the commit that carries this line)

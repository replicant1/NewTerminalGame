# WI-13 — launcher robustness (DEV-B, iteration M2)

Local mode. No push, no `gh`, no merge. Branch `wi-13-launcher-robustness`, cut from `main`.

05:50:01Z  START   WI-13. Re-scoped per plan §11.2 from first implementation to hardening and verification, because DEV-A built the A2 fallbacks into WI-1.
05:50:05Z  VERIFY  Re-read `main` myself rather than taking the sha on report, since it has now gone stale in three separate messages: `main` is at **`16b4dac`**, "Record the queue integration: nine branches, 256 to 575 tests". Cut the branch from there and ran the suite where I stand: **575 passed, 0 failed, 0 skipped**. Matches what I was told.
05:50:20Z  READ    launcher/lifecycle.py — `run`'s warning docstring, `open`, `has_live_processes`, `wait_until_idle`, `reap`, `_reap`. `launcher/desktop.py` — `is_busy` and `processes`. `launcher/script.py` — `window_is_busy` and `window_processes`.
05:50:30Z  NOTE    The machinery for the fix already exists: DEV-A built `script.window_processes`, `Desktop.processes` and `WindowLauncher.has_live_processes` in WI-3 and pointed `game.play` at them. What was left undone is that `run` and `wait_until_idle` still default to the `busy` flag, so the defect survives on the path the plan's own docstring tells people not to use. WI-13's first obligation is to remove the choice, not to add another helper.
05:50:40Z  PLAN    First obligation, before anything else: one answer in the system to "is this window's command still running". That means `wait_until_idle` polls the process list, `run` inherits it, and **`Desktop.is_busy` and `script.window_is_busy` are deleted** rather than left available — a second, wrong answer to one question is the trap the technical lead ruled on twice. Then the warning docstring goes, because a warning that outlives its defect teaches readers not to believe warnings in this code.
05:52:00Z  DRAFT   The first obligation, done. `wait_until_idle` now defaults to `has_live_processes`; `run` inherits it and its warning docstring is deleted; **`Desktop.is_busy` and `script.window_is_busy` are removed outright**, so there is exactly one answer in the system. The reap's failure message no longer says "still busy" either, since `busy` is now a removed concept: it says "something is still running in window %r".
05:52:10Z  TEST    578 passed, 0 failed, 0 skipped. 575 on main, minus the tests of the deleted API, plus replacements and three new ones — including two that assert the second answer is *gone*, so putting it back is a deliberate act.
05:53:00Z  VERIFY  **Real desktop, one window: `sleep 6` with the 40 x 30 grid applied. `busy` was TRUE for the whole life of the process and went false exactly when it ended. The defect did not reproduce.** Census 1 before, 1 after.
05:53:30Z  NOTE    Chased that rather than reporting the fix as verified on a defect I could not reproduce. Two probes with `python3 -m terminalgame.game_main` showed nothing running at all — because `do script` starts in the player's home directory and the module is not importable from there, so the game died instantly and the empty process list was honest. My own probe was wrong, not the launcher.
05:54:00Z  VERIFY  Re-ran with `launcher.game.game_command()` — what production actually sends, which waits for the grid, cds to the repository and execs. **The defect reproduces exactly: `busy` goes false at 0.75 s while `login|Python` runs on until 4.2 s — 9 lying samples in each of 2 trials. The process list was accurate for the whole span, and never once empty while something ran.** DEV-A's finding is confirmed.
05:55:00Z  VERIFY  Then isolated the cause, because "sleep with the grid" and "the game with the grid" disagreed. Four cells, one window each: **sleep/no-grid 0 lies of 9; sleep/grid 0 of 8; game/no-grid 0 of 15; game/grid 8 of 9.** So the defect needs **both** the grid and the game — the grid is necessary but not sufficient.
05:55:10Z  NOTE    That refines rather than contradicts WI-3's finding, which says "it is the grid alone". It matters: anyone who tried to keep `busy` by dropping the grid would see it work with a simple command and fail with the real game, and anyone testing the grid with `sleep` would conclude `busy` is fine. I am not editing DEV-A's finding; mine records the 2x2 and cites theirs.
05:55:15Z  NOTE    Windows opened so far: 13 (1 + 3 + 3 + 2 + 4). All closed by captured id, every close verified with `visible`, and the visible-window census returned to 1 after every probe.
06:03:00Z  DRAFT   tests/test_launcher_robustness.py — 31 tests. The regression at the level the defect was at (`run`, not `reap`), the one-answer property asserted across all four launcher modules, C2-beats-C3 revisited, `visible` not `exists`, and every call still bounded. The A2 fallbacks are NOT re-tested here: WI-1 already covers them and §11.2 says so.
06:03:30Z  NOTE    One of my assertions was wrong again and the code was right. I asserted a reap reports `closed=False` whenever any call refuses; it reports `closed=True` when only the *confirmation* refuses, with "could not be checked" in the reason. That is the better behaviour — reporting "not closed" would send a person hunting a window that is almost certainly gone — so I split the test to pin the distinction instead of flattening it.
06:04:53Z  TEST    609 passed, 0 failed, 0 skipped — `python3 -B -m unittest discover`, 8.3 s.
06:06:00Z  VERIFY  **The fix, proved on the real desktop through the production path.** WI-3's signature for the defect was that a session took the same ~1.0 s whether the game was asked for 5 seconds or 12, because it was killed either way. Ran `launcher.game.play` twice: **hold 3 s -> session 4.04 s; hold 8 s -> session 9.03 s. Holds differ by 5 s, sessions by 4.99 s.** The game now runs its full length and the window closes cleanly afterwards, `closed=True` both times.
06:06:05Z  NOTE    Windows opened in total: **15** (1 + 3 + 3 + 2 + 4 + 2). Every one closed by its captured id, every close verified with `visible`, and the visible-window census returned to 1 after every probe without exception. No window was ever closed while something was running in it, and no modal sheet was raised.

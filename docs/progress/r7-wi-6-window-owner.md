01:56:33Z  WI-6  START   developer C picks up WI-6 (the window owner) on branch r7/wi-6-window-owner cut from main at 9863a95, carrying the WI-5 log tail and the M0 lane C completion record forward as the conductor ruled
01:57:09Z  WI-6  RISK    about to map ONE window for under a second to answer the conductor flagged risk: if the event loop shares the path that hangs update(), WI-7 finds it as a hang on the user desk instead of a test failure. My own root, after() schedules the destroy BEFORE mainloop is entered so the loop has an exit path before it has a chance to need one, faulthandler watchdog at 15 s with exit=True, nothing calls update()
01:57:17Z  WI-6  VERIFY  THE RISK IS CLOSED. mainloop() works on a MAPPED window and returns cleanly when the window is destroyed from inside an after() callback — 629 ms, exactly as scheduled. update_idletasks() on a mapped window returns in 10.6 ms. So the event loop does NOT share the path that hangs update(); WI-7 will not find this on the user desk. While mapped: geometry 400x570+5+38, winfo size (400,570), title "Terminal Game" — WIN-2 and WIN-3 confirmed on a real window. Window reaped, no stray process, visibleApplicationCount back to 7
01:57:17Z  WI-6  NOTE    trap for anyone writing a mapped-window test: on Tk 8.5 Aqua a non-resizable mapped toplevel reports state() == "zoomed", NOT "normal". A test asserting "normal" would fail for no good reason. Use winfo_ismapped/winfo_viewable instead
01:57:17Z  WI-6  VERIFY  withdrawn toplevel reports: winfo_reqwidth/reqheight 400x570 (usable), but winfo_width/height 1x1 and geometry() "1x1+5+38" (useless while unmapped). title() round-trips exactly. After destroy a CHILD widget winfo_exists() is False cleanly, but the ROOT raises TclError "application has been destroyed" — close detection has to handle both
01:57:59Z  WI-6  VERIFY  caught a real bug in my own is_resizable() before writing its test: root.resizable() answers with the Tcl STRING "0 0", not a pair of booleans, and every character of that string is truthy — so "any(bool(flag) for flag in ...)" reported True for a window that cannot be resized at all. Fixed to split and int() the flags
01:57:59Z  WI-6  VERIFY  a GameWindow Tk root coexists with the conftest session root: three windows built and closed in a row beside it, all reporting title "Terminal Game", requested size (400,570), ground #000000, and the session root still alive afterwards. So WI-6 can own its root, as production needs, without disturbing WI-5 fixtures
02:00:07Z  WI-6  VERIFY  my own test segfaulted the interpreter: driving Tcl directly with root.tk.dooneevent(0) to turn the loop by hand. All 44 tests passed and then the process died in that call. Removed, not worked around
02:00:07Z  WI-6  VERIFY  FINDING WORTH PASSING ON to lanes A and B: mainloop() runs on a WITHDRAWN root, after() callbacks fire inside it, quit() returns from it in 60 ms, the root survives, and state stays withdrawn with ismapped and viewable both False. So the event loop and the timer can be driven HEADLESSLY in the default suite — WI-14 cadence and WI-16 journeys do not need a real window
02:00:07Z  WI-6  VERIFY  Tk normalises binding names when reporting them: "<Key-q>" comes back as "q" and "<Up>" as "<Key-Up>". Also event_generate on a withdrawn toplevel delivers NOTHING, so key delivery cannot be tested headlessly — that test moved to the needs_window class, where the window is mapped and focused
02:02:07Z  WI-6  RISK    about to run the two needs_window tests deliberately, which map a real window for about 0.3 s each. Both schedule their own close via after() before show(), plus a 5 s belt-and-braces close, and the fixture reaps on the failure path
02:04:33Z  WI-6  BLOCKED-NO-LONGER  my own needs_window run HUNG with a window on the user screen and I killed it at 02:04:17Z (pid 41837, my own process, killed by pid not by name). visibleApplicationCount was 8 during the hang and is back to 7; no stray process. Caught within two minutes because I checked the desktop immediately when the command timed out
02:04:33Z  WI-6  VERIFY  ROOT CAUSE of that hang, and it is a real defect: mainloop() belongs to the INTERPRETER, not the window. close() destroys self._root, so when GameWindow owns the interpreter the root goes and mainloop returns — production is fine. But when the window is a Toplevel on somebody else roots, close() destroys only the Toplevel, the root survives, and mainloop NEVER RETURNS. That is a window on a desk with no exit path, which is the exact thing ground rule 1.5 exists to prevent
02:04:33Z  WI-6  DECIDE  how to make that impossible rather than documented -> close() now calls quit() BEFORE destroy(), unconditionally. run() then returns on close whoever owns the interpreter. The guarantee stops depending on who constructed the window
02:05:07Z  WI-6  VERIFY  fix confirmed by the very tests that hung: "pytest -q -m needs_window" now 2 passed, 470 deselected, in 1.14 s, run under an EXTERNAL 45 s subprocess deadline so it could not hang again. No stray process, visibleApplicationCount back to 7. Default suite 470 passed, 0 failed, 0 skipped, 2 deselected
02:05:07Z  WI-6  NOTE    lesson for me, not just for the code: I ran a window-mapping suite with no deadline outside pytest. The window hung on a real desk for about two minutes. From here every needs_window run goes inside a subprocess timeout, which is what caught it the second time
02:06:00Z  WI-6  VERIFY  CRASH ACCOUNTING, measured not remembered. 14 Python .ips today: 3 are the ctypes incident (01:38:55Z, 01:39:19Z, 01:39:24Z) and 11 are mine from WI-6, all between 01:58:24Z and 02:00:51Z. Signatures: 4 are EXC_BAD_ACCESS with Tcl on top (115824, 115850, 115857, 120051.000) and 7 are EXC_BAD_ACCESS/SIGSEGV or SIGBUS with libsystem_c on top (115844, 115937, 115943, 115949, 120021, 120027, 120051). All eleven are the SAME defect diagnosed over about eleven runs of tests/test_window.py in two and a half minutes — not eleven distinct mistakes
02:06:00Z  WI-6  NOTE    WHY MY LOG NAMED ONLY ONE, which is the part worth fixing: the libsystem_c ones crashed at INTERPRETER EXIT, after pytest had already printed its results. The command output looked like a completed run, so I did not see a crash to log. Only the one that killed pytest mid-test was visible to me. Lesson: the command output is not a reliable crash detector; the .ips directory is, and I should have been counting it
02:06:00Z  WI-6  VERIFY  crashing is ELIMINATED for this shape, not merely avoided, and the evidence is a count: ZERO new .ips since 02:00:51Z across six full-suite runs and one needs_window run between 02:01:40Z and 02:05:00Z. Two paths closed — the dooneevent Tcl call is deleted, and one-interpreter-per-process means the suite can no longer create a second Tk root. Residual, stated plainly: anyone who constructs a second tkinter.Tk() in the same process can still segfault this build
02:06:51Z  WI-6  TEST    586 passed, 0 failed, 0 skipped, 2 deselected  (.venv/bin/python -m pytest -q, after merging origin/main a2373a9 with no conflict). needs_window separately: 2 passed
02:06:51Z  WI-6  DRAFT   docs/prs/PR-WI-6-window-owner.md
02:07:14Z  WI-6  COMMIT  ec3563c WI-6: the window owner — pushed, draft PR #90 open
02:08:16Z  WI-6  MERGE   PR #90 merged to main as ea00019 at 02:07:39Z; fast-forwarded and re-ran the whole suite on ea00019 -> 586 passed, 0 failed, 0 skipped, 2 deselected. visibleApplicationCount 7, crash reports still 14 (none new). main is green with WI-6 in it
02:08:16Z  WI-6  DONE    WI-6  r7/wi-6-window-owner  merged as ea00019; lane C has now landed S-2, WI-5 and WI-6

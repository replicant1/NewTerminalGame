WI-17  02:21:02Z  START   WI-17 the human-verification pack begun; branch r7/wi-17-human-verification cut from the tip of origin/main, da9ec9c.
WI-17  02:21:02Z  NOTE    WI-14's log tail carried forward. WI-14 merged as da9ec9c (PR #97); main was green at 886 passed, 5 deselected.
WI-17  02:21:02Z  VERIFY  CRASH BASELINE: 14 Python-*.ips reports. Suite baseline: 886 passed, 0 failed, 0 skipped, 5 deselected.
WI-17  02:21:02Z  PLAN    wait for lane B's checklist (r7/wi-17-checklist-from-lane-b) before writing the human pack — it is the designed input and carries what must NOT be claimed. Meanwhile do the parts that are mine and defined: extend the key-delivery test to the four arrows, and run every check an agent can run.
WI-17  02:21:02Z  NOTE    reusing WI-7's window pattern rather than writing a second one. That is the stated reason the item stayed in lane A: the risk here is windows, not knowledge, and a second pattern is the hazard.
WI-17  02:22:19Z  VERIFY  FOUND A DEFECT IN MY OWN LANDED WI-14 while checking lane B's WIN-4 claim: the assembled game put its window at Point(5,38), Tk's default, while placement_for says Point(556,206). Nothing in the product called move_to or placement_for. Lane B thought the fallback was running; it was not. Reported at once rather than folded in; the conductor dispatched it as WI-14b, and then WI-14c for the injection seam lane C needed.
WI-17  02:26:40Z  NOTE    the technical lead's root cause, which overrules my "explanation and not a defence": WI-15 is not among WI-14's dependencies and WI-15's only outgoing edge goes to WI-17, a verification item, which consumes nothing. So WI-15 produced a capability no item was told to use. Recorded here because the next person in game.py should know the assembly is where a capability gets connected.
WI-17  02:27:30Z  VERIFY  converted a human check into an agent check, which is this item's purpose: Tk reports the four arrow keysyms as Up/Down/Left/Right, exactly WI-13's table, on a real mapped window — asserted THROUGH intent_for so it pins the whole path rather than the raw keysym, as lane B suggested. Only the physical keypress stays human.
WI-17  02:28:13Z  VERIFY  all agent-checkable facts gathered in ONE window rather than six, because each is a disturbance on somebody's desk: mapped True, title "Terminal Game", size (400,570), ground #000000, placed at the WI-15 point and not at Tk's (5,38), and an absolute coordinate round-tripping (600,300) in and out. 3 passed in 1.27s under a 90s deadline outside pytest. Crash reports 14 before and 14 after, zero new, nothing left behind.
WI-17  02:28:13Z  NOTE    lane B's 1.4 residual is only half closeable by an agent and I did not overclaim it: I confirmed Tk ACCEPTS and REPORTS BACK an absolute coordinate, which is what WI-15 showed. Whether Tk's (0,0) is the display server's (0,0) needs an eye or a ruler, so it stays in the pack — as part of the COST of the user granting Accessibility, since it is dormant while the reader reads nothing and load-bearing the moment one does.
WI-17  02:29:10Z  DRAFT   docs/findings/WI-17-human-verification.md — five checks in lane B's failure-shape form, the WIN-4 decision with its cost, what an agent checked, what must not be claimed (including by me), and a per-code status table for WI-18.
WI-17  02:29:10Z  NOTE    I did NOT run the real entry point. python -m terminal_game.shell.game has no watchdog by design — GAME-3 forbids a time limit — so it waits for a key only a person can press. Running it would have hung with a window on the user's desk. Verified the entry point resolves and main() is callable without invoking it.
WI-17  02:29:40Z  TEST    925 passed, 0 failed, 0 skipped, 8 deselected — `.venv/bin/python -m pytest -q`. WI-17 adds 1 headless guard and 3 needs_window tests. Crash reports 14, unchanged across the whole item.

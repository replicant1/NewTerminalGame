05:08:28Z  START   WI-1a, moving the specimen fixture out of tests/, on branch r6/wi-1a-specimen-home off main. Amendment 2 ruled it must move and made the destination DEV-Bs call; DEV-C asked for it on PR #45 because WI-10 rule 6 cannot land while it stands
05:08:28Z  DECIDE  destination -> terminal_game/presentation/specimen.py, because the dependency then runs the right way for BOTH tests and tools (both import down into the package, nothing imports out of tests/), because it is a picture and this is the layer whose job is to reproduce it, and because it adds no new top-level package for WI-10 rule 4 to have to know about
05:08:28Z  DECIDE  moved it AND fixed all five importers including DEV-Cs tools/walking_skeleton.py, rather than leaving their one line to them, because a rename that left main broken between two landings is worse than touching one import in another lane file. DEV-C offered either way on PR #45
05:08:28Z  NOTE    found and fixed a violation of my own while here: tests/test_frame.py (WI-1, written before WI-13 existed) contained the STAT-2 literal. A7 confines every status-line literal to WI-13. Replaced with a non-literal
05:08:28Z  NOTE    for DEV-C and WI-10: the specimen module still contains the STAT-2 text, as SPECIMEN_STATUS_ROW. That is a transcription of the requirements PICTURE, which is the evidence behind contradiction C-3, not a template anybody composes from. A naive literal-scan will flag it; it should not
05:08:28Z  VERIFY  grep for "from tests." across terminal_game/ and tools/ -> NONE. WI-10 rule 6 now holds on the tree
05:08:28Z  TEST    533 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"]

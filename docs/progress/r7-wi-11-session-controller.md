WI-11  02:05:49Z  START   WI-11 the session controller begun; branch r7/wi-11-session-controller cut from the tip of origin/main, a2373a9.
WI-11  02:05:49Z  NOTE    WI-4b's log tail travels with this branch, uncommitted from the last one. WI-4b merged as d06c71b (PR #88); main was green at 488 passed afterwards.
WI-11  02:05:49Z  NOTE    lane C answered the exit-path question (relayed): WI-6 owns the GUARANTEE, WI-7 owns the POLICY. WI-6 gives bind_key and close(); the constructor wires WM_DELETE_WINDOW to close() unilaterally so a window can never be left with no way out; close() calls quit() before destroy() so run() returns whoever owns the interpreter. My watchdog plan is endorsed. Not WI-11's business, but recorded here because it is the answer to the ASK I left open on the last branch and it must not be lost.
WI-11  02:06:10Z  VERIFY  baseline before writing anything: `.venv/bin/python -m pytest -q` -> 534 passed, 0 failed, 0 skipped. Exactly the count the conductor named.
WI-11  02:06:10Z  READ    plan section 5 (WI-11), WI-8's domain/state.py (GameState, Outcome, new_game), WI-10's application/turn.py (Intent, outcome_of, resolve_player_intent, resolve_ghost_move), WI-9's ghost policy.
WI-11  02:06:30Z  DECIDE  the enum is called Phase, not State, because GameState is already the board. Its three members are the plan's three states verbatim.
WI-11  02:06:30Z  DECIDE  the session owns the GHOST'S HEADING. Nobody claimed it — I flagged it as unclaimed in WI-9's PR — and the session is the natural owner because it is the thing that persists between ticks. INITIAL_GHOST_HEADING = EAST: arbitrary, nothing in the specification chooses, but FIXED and pinned by a test so a seeded run reproduces.
WI-11  02:06:30Z  DECIDE  a tick must actually move the ghost, or END-5 ("in Decided a tick moves nothing") could not be tested at all. So the session calls WI-9's policy and carries the heading. Randomness arrives as an argument; no clock, no timer — the beat arrives as a call.
WI-11  02:06:30Z  DECIDE  WIN-5's seam to the Shell is an `on_end` callback, not a process exit. Application may not name sys or a toolkit (layer rule 1.3), so reaching Ended sets is_running False and calls back. WI-7/WI-14 wire it to window.close(). Additive; flagged in the PR.
WI-11  02:07:40Z  NOTE    a test of mine failed and it was the CODE that was wrong, not the test. GameState.outcome is a STORED field that the resolver stamps; turn.outcome_of is the DERIVED total function. A hand-built board can have the player and the ghost on one square and still carry UNDECIDED. My first draft read the field, which would have let such a board be PLAYABLE — and since Decided is what holds END-3 up, that is exactly the crack I was told to keep shut.
WI-11  02:08:05Z  DECIDE  the session asks outcome_of and never reads the stored field. One source of truth, which is what structural END-3 means. Consequence, pinned by a test: a board stamped CAUGHT whose actors are a square apart is treated as PLAYING, because the rules are authoritative and the stamp is a cache of them. In production the two can never disagree — the resolver always stamps what outcome_of returned.
WI-11  02:08:25Z  NOTE    discipline applied throughout this file, per the lead's generalisation: EVERY "nothing happened" test is paired with a control proving the same action DOES happen while Playing. A tick moving nothing in Decided is worthless unless the identical tick on the identical board visibly moves the ghost in Playing.
WI-11  02:08:25Z  TEST    560 passed, 0 failed, 0 skipped — `.venv/bin/python -m pytest -q` from the repository root. 534 were already there; WI-11 adds 26.
WI-11  02:08:55Z  COMMIT  6683b5b WI-11: the session controller
WI-11  02:08:55Z  COMMIT  1030774 WI-11: PR summary
WI-11  02:08:55Z  VERIFY  pushed r7/wi-11-session-controller; draft PR #91 opened against main -> https://github.com/replicant1/NewTerminalGame/pull/91
WI-11  02:09:20Z  NOTE    merged origin/main before marking ready; it brought WI-6 (lane C, the window owner) with it. Clean merge, no conflict. The count line gains "2 deselected" from here on — WI-6's needs_window tests, excluded by pytest.ini as section 1.6 requires. 560 -> 612 passed.
WI-11  02:09:30Z  MERGE   PR #91 merged into main as a356a86. 612 passed, 0 failed, 0 skipped, 2 deselected at the moment of merge.
WI-11  02:09:42Z  VERIFY  main confirmed green afterwards: fast-forwarded 61c57f0..a356a86; `.venv/bin/python -m pytest -q` -> 612 passed, 0 failed, 0 skipped, 2 deselected.
WI-11  02:09:42Z  TEST    612 passed, 0 failed, 0 skipped, 2 deselected
WI-11  02:09:42Z  DONE    WI-11 r7/wi-11-session-controller a356a86 (PR #91 merged as a356a86). The last lines are uncommitted on purpose: the tail is carried forward on the next branch I cut, which should be WI-7 now that WI-6 has landed.

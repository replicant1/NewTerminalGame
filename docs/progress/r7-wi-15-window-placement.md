# WI-15 — where the window lands (developer B, run 7)

WI-15 START   02:11:06Z  developer B begins WI-15 on branch r7/wi-15-window-placement, cut from main at 2599b1a. Non-local mode, lane B, iteration M2. Moved from lane C, which keeps WI-12 and WI-16. The anchor-reading route is an unanswered question with the user, so I build everything that does not depend on it and choose nothing. Carrying the WI-13 log tail forward.
WI-15 VERIFY  02:11:19Z  crash reports before starting: 14 Python-*.ips in ~/Library/Logs/DiagnosticReports, newest 2026-09-17-120051, none since. Matches the conductor's count
WI-15 VERIFY  02:11:28Z  suite baseline on the branch: 705 passed, 0 failed, 0 skipped, 2 deselected
WI-15 VERIFY  02:12:17Z  S-2 item 6 settled: Tk DOES accept a negative absolute origin, '+-877+-1348' round-trips exactly. But '+{}+{}'.format(x,y) and '{:+d}{:+d}'.format(x,y) are NOT the same string - the second gives '-877-1348', which Tk reads as 877 from the RIGHT edge and 1348 from the BOTTOM. Measured both forms reading back differently on the withdrawn root. The naive sign format is the trap, not the negative value
WI-15 VERIFY  02:12:17Z  Tk's winfo_screenwidth/screenheight report 1512x982 and vrootx/vrooty report 0,0 - the MAIN display only, with no knowledge of the two displays at negative origins. So any clamp to the Tk screen would reject valid anchors on those displays. Decision: do not clamp
WI-15 NOTE    02:14:02Z  my own test caught a false claim in my own docstring: I had written that a freshly built window has no position. It has one - Tk gives a fresh toplevel (5,38) on this build. So position() answering something is NOT evidence that anything placed the window, and a test checking placement by asking whether a position exists would pass without the placement code running. Docstring corrected, test rewritten to move to distinctive coordinates
WI-15 TEST    02:14:02Z  763 passed, 0 failed, 0 skipped, 2 deselected  (.venv/bin/python -m pytest -q from the repository root; 705 inherited, 58 new)
WI-15 VERIFY  02:14:02Z  crash reports after all WI-15 work: still 14, newest still 2026-09-17-120051. No new Python-*.ips. No permission dialog appeared at any point; nothing queried the desktop, no AppleScript was run and no ctypes was used

05:13:21Z  WI-14 START   DEV-C, WI-14 the anchor; branch r6/wi-14-anchor cut from origin/main 67de3f1; r6/wi-14-* empty on origin. Conductor holds the screen gate and DEV-B then DEV-A are ahead of me in the queue.
05:14:24Z  WI-14 DECIDE  the anchor query -> Tk's mouse pointer position, not another application's window, because on this machine the latter needs Accessibility or Automation permission that nobody has granted and whose first use raises a modal dialog. A2 says use what the game can see without a prompt; the pointer is what that is. Deviation from A2's word 'window', flagged for a ruling.
05:14:27Z  WI-14 DRAFT   terminal_game/shell/anchor.py (pure: ScreenBounds, Anchor, WindowAnchor, brought_onto_screen, no_anchor) and terminal_game/shell/tk_anchor.py (the only module here that names Tk; withdrawn root, destroyed in a finally, maps nothing)
05:15:14Z  WI-14 TEST    599 passed, 0 failed, 0 skipped -- /usr/bin/python3 -m unittest discover -t . -s . -p test_*.py ; 25 new over the 574 baseline. All six WI-10 rules still clean.
05:15:48Z  WI-14 COMMIT  2abfd2b WI-14: the anchor, and the query that asks nobody for permission -- pushed; draft PR #54 opened
05:15:52Z  WI-14 BLOCKED waiting on the conductor's screen gate before running tools/probe_anchor_query.py. Everything that needs no screen is done and green. The probe withdraws its Tk root before anything can be mapped and enters no event loop, so I believe it shows nothing and takes no focus -- but the rule says ask, so I am asking rather than deciding for myself.
05:16:11Z  WI-14 NOTE    conductor granted the screen gate exclusively (DEV-B then DEV-A released; project ledger 21 opened, 21 reaped). Running tools/probe_anchor_query.py now.
05:16:55Z  WI-14 VERIFY  real probe, Tk 8.5.9 aqua: pointer at (-175,-448), stable over 5 reads, while winfo_screenwidth/height and winfo_vrootwidth/height all report 1512x982 -- the PRIMARY display only. maxsize() reports (5120,2422), a different and larger figure with no origin. So the pointer is on a second display up-and-left, and Tk 8.5 will not give its bounds.
05:16:59Z  WI-14 DECIDE  an anchor outside the primary display's bounds -> treated as nothing seen, because it cannot be clamped and WIN-4's purpose is that the window lands somewhere VISIBLE. Replaces a guard I had written on a guess (that Tk answers -1,-1 when it cannot say) with one written on a measurement.
05:17:56Z  WI-14 VERIFY  probe run twice on the real toolkit. Query returned None in 115.0 ms and 118.6 ms -- a TCC dialog blocks its caller until a human answers, so a sub-120ms return is evidence none appeared. No dialog was seen. tkinter._default_root None afterwards; pgrep shows no leftover process of mine.
05:18:00Z  WI-14 TEST    603 passed, 0 failed, 0 skipped -- /usr/bin/python3 -m unittest discover -t . -s . -p test_*.py ; 29 new over the 574 baseline; all six house rules clean
05:18:28Z  WI-14 DRAFT   docs/findings/WI-14-anchor-query.md -- records the dialog evidence, the multi-display obstacle that A2's permission would not clear, the defect the probe found in my own guard, and the section 4 contradiction
05:19:17Z  WI-14 TEST    634 passed, 0 failed, 0 skipped after merging origin/main (WI-16, WI-17 and their records landed); clean merge, no conflict; all six house rules still clean

WI-14c  02:25:27Z  START   WI-14c the anchor seam; branch r7/wi-14c-anchor-seam cut from the tip of origin/main, a4245a2. WI-14b had already merged without an injection point, and lane C is holding WI-16b with 9 tests red on exactly that.
WI-14c  02:25:27Z  DECIDE  the seam is `anchor_reader=None` on build_game AND on Game, defaulting to placement.no_anchor(), passed to placement_for in Game.place(). Lane C offered to build to whatever I chose; I took the shape it asked for, because it is already coding against it and any cleverness of mine would cost it a rewrite for no gain.
WI-14c  02:25:27Z  NOTE    the argument for it is stronger than convenience and is now in the docstring. Ground rule 1.6 forbids the default suite from querying the desktop, so a placement test cannot use the shipped reader; without a seam the only test of placement is one marked needs_window, excluded by default, and therefore unable to catch the very defect WI-14b existed to fix. A capability with no way to test it by default is how that defect survived.
WI-14c  02:26:11Z  TEST    925 passed, 0 failed, 0 skipped, 7 deselected. Two tests added: the game ASKS the reader it was given and uses the answer, and the CONTROL that two different anchors give two different positions — a game that asked and then ignored the reply would pass the first alone if the fallback happened to agree.

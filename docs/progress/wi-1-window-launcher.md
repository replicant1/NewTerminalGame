# WI-1 — the window launcher and the desktop automation adapter

Branch `wi-1-window-launcher`, cut from `main` at `d4c1a7f`. DEV-A, iteration M0. Local mode.

04:51:19Z  WI-1 START   DEV-A begins WI-1 — the window launcher and the desktop automation adapter, the only component that touches the desktop.
04:51:55Z  WI-1 READ    docs/IMPLEMENTATION_PLAN.md in full — §2 runtime, §3 layer rule, §4 ground rules, §7 M0 and the WI-1 row and test obligations.
04:52:02Z  WI-1 READ    docs/ARCHITECTURE.md §2 Candidate 1 (structure, both use cases), §8 assumptions A1-A10, §9 cautions C1-C14, §10 open questions Q1-Q3.
04:52:05Z  WI-1 READ    docs/FUNCTIONAL_REQUIREMENTS.md §2 — WIN-1 to WIN-5, the five codes WI-1 answers.
04:52:08Z  WI-1 NOTE    Branch cut from main d4c1a7f. Working tree holds only docs, orchestration and the five stale root executables; no application code, as §1 of the plan says. The stale executables are not opened.
04:52:20Z  WI-1 VERIFY  Terminal.app scripting dictionary read from /System/Applications/Utilities/Terminal.app/Contents/Resources/Terminal.sdef (sdef(1) itself needs Xcode and is unavailable; the bundle file is readable).
04:52:25Z  WI-1 VERIFY  Dictionary says: `window` carries id, position, size, bounds, frame, visible — and NOT rows/columns/title. `tab` carries number of rows, number of columns, busy, custom title, the five `title displays ...` switches, background color, normal text color, font name, font size, tty, processes. So 40x30, the title, the black ground and the font are all tab properties reached as `selected tab of window id <ID>`.
04:52:30Z  WI-1 VERIFY  `do script` returns a **tab**, not a window — there is no `window of tab` property, so the identity capture has to go through `first window whose tabs contains <thatTab>`. That construct is the one genuinely uncertain thing in the design and is probed next.
04:52:34Z  WI-1 DECIDE  Per-window settings only -> every appearance property is set on the tab instance, never on a `settings set`, because a `settings set` is a saved profile and assumption A3 forbids touching the player's saved preferences.
04:53:10Z  WI-1 VERIFY  Probe 1 against the real desktop (one window, bounded, reaped): `set theTab to do script "..."` then `id of (first window whose tabs contains theTab)` captured id 7653 at the moment of creation. The by-id specifier `window id 7653` works for name, position, size, the tab and close. 40x30 on the tab gave a window of 357 x 558 pixels at Menlo 14 (a default 80x24 window was 597 x 385). `visible of window id 7653` after close returned **false** rather than erroring, so the close verification the brief asks for is available.
04:53:14Z  WI-1 VERIFY  Probe 1 also measured `position of window id 7653` = **-879, 84** before the move — this machine has a display to the left of the main one, so window coordinates are legitimately negative and a naive clamp to the main display would teleport the game window onto another screen.
04:53:40Z  WI-1 VERIFY  Probes 2 and 3 isolated the window title. With all five scriptable `title displays ...` switches off the title still read `rodneybailey — sleep 6`; turning custom title on gave `rodneybailey — Terminal Game — sleep 6` while busy and `rodneybailey — Terminal Game` once idle. So two title components are **not** reachable from the scripting dictionary: the working directory the login shell publishes, and the active process name.
04:53:55Z  WI-1 RISK    WIN-3 ("the window is titled Terminal Game") cannot be met exactly through AppleScript alone while the player's saved Terminal profile has the non-scriptable title components switched on, and assumption A3 forbids me changing that profile. The launcher sets the custom title correctly; the residue is a human check and a finding.
04:54:04Z  WI-1 PLAN    Build `launcher/` as five modules: pure geometry, a pure AppleScript source builder, a runner seam over `osascript`, a typed desktop adapter, and the lifecycle policy. Tests drive the policy through a recording fake runner and assert on the **AppleScript text that would reach the desktop** — which is the real artefact — plus pure arithmetic tests for the offset and the clamp.
04:56:20Z  WI-1 DECIDE  Package name -> `launcher/`, tests in `tests/` with an empty `tests/__init__.py`, because §3 of the plan leaves naming to the developers and `launcher` says plainly that nothing in the game may import it (caution C11). DEV-B's WI-2 screen port needs a different package, so there is nothing to collide over.
04:56:25Z  WI-1 DECIDE  The test seam -> the subprocess runner, not the adapter, because it puts the **AppleScript source text** in front of the tests. "Which window does this script name" is a fact about the text; "was a method called" is not, and C1 is exactly a claim about the text.
04:56:30Z  WI-1 DECIDE  The visibility guarantee -> clamp the new window's corner into the **reference window's own frame** first, then fit the whole window into the screen rect. The screen rect from the desktop is the union of all displays (measured -3509,-1440,1611,982), much of which is over no display, so it is a backstop and not a guarantee; the reference window is, because the player was just looking at it.
04:56:35Z  WI-1 TEST    11 passed, 0 failed, 0 skipped  (python3 -m unittest discover — geometry only so far)

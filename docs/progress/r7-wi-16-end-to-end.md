02:13:53Z  WI-16  NOTE    standing by for WI-14 public surface before branching. Using the wait to read the assembled pieces WI-16 will drive, looking for problems rather than writing code that would collide with lane A
02:14:34Z  WI-16  NOTE    trap for my own item, not a defect: on a maze with few junctions the ghost never consults the random source at all — seeds 7 and 8 gave an identical walk on my 2-loop fixture. WI-16 must not assert "different seeds give different playthroughs" without a maze that actually has junctions
02:14:34Z  WI-16  VERIFY  determinism confirmed for my own planning: ghost candidates are a LIST ordered by Direction declaration order, and one seed walks the same 300 steps twice. INITIAL_GHOST_HEADING is EAST as advertised
02:14:34Z  WI-16  VERIFY  found a defect in lane A WI-11 while reading ahead: Session.__repr__ reads self._game.outcome, the stale stamped field that the whole rest of the module is careful never to read. Measured on a board of the kind WI-16 builds — both actors on one square, field left UNDECIDED: phase=decided and outcome=caught (both correct, both ask the function) but repr says "Session(decided, undecided, score 37)". repr is what pytest prints on failure, so WI-16 hand-built boards would show a misleading outcome in every traceback. Raised on PR #91, not fixed by me: lane A module, WI-14 live in it
02:15:07Z  WI-16  DECIDE  how to close it -> add status_cells() to my own status.py rather than ask lane A to change compose_frame. It keeps the padding decision inside WI-12 where the ruling puts it, works with compose_frame exactly as it stands so WI-14 is unblocked without anyone touching lane A code, and leaves the text-and-colour signature open if lane A still prefers it
02:15:07Z  WI-16  VERIFY  GAP between two merged modules that WI-14 will hit next: compose_frame still takes status_row as Sequence[Cell] of exactly 40, status_text returns a str, and NOTHING bridges them. So WI-14 would have to pad row 29 to 40 itself — which is composing part of row 29 outside WI-12, the one thing the lead forbade
02:17:59Z  WI-16  START   developer C picks up WI-16 (the end-to-end behaviour suite) on branch r7/wi-16-end-to-end, STACKED on r7/wi-14-live-assembly at 46f9682 per plan section 7. Carrying the WI-12b log tail forward. PR will target the parent branch, retargeted to main once WI-14 merges
02:19:15Z  WI-16  VERIFY  END-3 board found and it is as small as it can be: TWO corridor squares. new_game puts the player on one and the ghost on the other, with the only dot on the ghost square. One move takes the player onto the ghost, eating the last dot AND colliding — score 1, dots 0, player == ghost, outcome CAUGHT. That is precisely the board where the two step orderings disagree, and it confirms the lead ruling that the last dot on the ghost square does score
02:19:15Z  WI-16  VERIFY  found both full-game fixtures, each a real playthrough with the ghost ticking. WIN: a 7x5 ring, player follows the corridor starting WEST (the way the ghost circulates) -> cleared after 19 turns, score 19, 0 dots left. LOSS: the same ring, player starts EAST, straight into the ghost -> caught after 7 turns, score 7. Both identical across seeds 0, 1 and 7, which is my own junction trap showing up exactly as predicted — a ring has no junctions so the seed never matters
02:20:34Z  WI-16  NOTE    consequences. (1) Every "visibleApplicationCount 7 before and after" I reported shows no stray APPLICATION was left registered, which is real and worth having, but it does NOT show that no window reached the screen — I presented it as if it did. (2) The reading of 8 right after my suite run at 02:19:50Z was the pytest process own Tk root, not a leaked window. (3) The baseline itself drifts with what the user is doing — it was 7 earlier and is 8 now — so only the DELTA either side of a run means anything. (4) What actually evidences no window is the Tk-level assertion: state()==withdrawn, winfo_ismapped False, winfo_viewable False. Those are in the tests and are unaffected
02:20:34Z  WI-16  VERIFY  CORRECTION TO MY OWN EVIDENCE, and I have been repeating it all run. lsappinfo visibleApplicationCount counts REGISTERED APPLICATIONS, not windows. Measured just now: baseline 8, then 9 while a WITHDRAWN Tk root that never maps anything is alive, then 8 again when it exits. So the count rises for any live Tk process — it is S-1 Dock tile, not a window
02:20:54Z  WI-16  DRAFT   docs/prs/PR-WI-16-end-to-end.md
02:20:54Z  WI-16  TEST    890 passed, 0 failed, 0 skipped, 4 deselected  (.venv/bin/python -m pytest -q on the stacked branch; WI-16 adds 33). Crash reports 14, none new
02:22:04Z  WI-16  NOTE    CONFLICT on merging origin/main da9ec9c, add/add in my OWN two progress logs. Cause: the read-ahead half of the WI-16 log reached main inside the WI-12b PR, while the branch half was written on a branch cut from 46f9682, which predates it. Neither side was a superset — they were nearly disjoint — so the resolution is the UNION in timestamp order, 13 lines, no line dropped. The WI-12b log was a clean superset and took mine. Nobody else touched either file
02:22:04Z  WI-16  TEST    919 passed, 0 failed, 0 skipped, 5 deselected — whole suite after merging origin/main da9ec9c, which brought WI-14
02:23:41Z  WI-16  MERGE   PR #99 merged to main as b302b2d at 02:23:04Z, base main (WI-14 had merged so no retarget was needed); fast-forwarded and re-ran the whole suite on b302b2d -> 919 passed, 0 failed, 0 skipped, 5 deselected. Crash reports still 14, none new since 02:00:51Z
02:23:41Z  WI-16  DONE    WI-16  r7/wi-16-end-to-end  merged as b302b2d

05:05:30Z  WI-10 START   DEV-C, WI-10 the rules of the house enforced; branch r6/wi-10-house-rules cut from origin/main b7dd6ea (WI-12 frame composer landed); r6/wi-10-* empty on origin
05:06:16Z  WI-10 VERIFY  swept the tree against all six rules before writing any -- rule 6 is violated today by tools/walking_skeleton.py:208 'from tests.specimen import SPECIMEN_ROWS', the exact case amendment 2 names; the other five hold
05:06:21Z  WI-10 ASK     DEV-B, by gh pr comment on PR #45: where should the specimen fixture live once it leaves tests/? Amendment 2 ruled it must move and made the destination DEV-B's call. https://github.com/replicant1/NewTerminalGame/pull/45#issuecomment-5692330566
05:06:24Z  WI-10 ASSUME  building all six rules now; if the fixture has not moved by merge time I land rules 1-5 green and report rule 6's tree-check as a known open violation in docs/findings/, rather than baking a silent exemption into the guard or choosing DEV-B's file name. Affects only the one test method and one findings note.
05:08:17Z  WI-10 DRAFT   tests/house_rules.py -- six rules as functions returning a Report(rule, inspected, violations); every check parses with ast, never greps, because grid_surface.py's own docstring lists the forbidden create_* names and a text scan would fail the file whose documentation keeps the rule
05:08:21Z  WI-10 DECIDE  vacuity -> baked into the Report rather than left to the test: a rule that inspected no files carries a violation saying so, so no rule can go green over an empty set however it is called
05:09:42Z  WI-10 NOTE    DEV-B answered on PR #45 by landing it: PR #50 'WI-1a: the specimen fixture leaves tests/ (unblocks WI-10 rule 6)' moved tests/specimen.py to terminal_game/presentation/specimen.py. Rule 6's violation is gone; all six rules now clean on main. Block never became a BLOCKED line.
05:09:46Z  WI-10 VERIFY  all six rules clean over the tree after merging origin/main c4198e4 -- rule 1 over 19 files, rule 2 over 9, rule 3 over 30, rule 4 over 57, rule 6 over 30, rule 5 over the live interpreter
05:09:50Z  WI-10 TEST    574 passed, 0 failed, 0 skipped -- /usr/bin/python3 -m unittest discover -t . -s . -p test_*.py ; 41 of them WI-10's
05:10:36Z  WI-10 NOTE    draft PR #52 opened -- https://github.com/replicant1/NewTerminalGame/pull/52 , base main
05:11:18Z  WI-10 READ    amendment 4 landed while I worked: WI-17 moves DEV-C -> DEV-A; WI-20a/WI-20b stay with DEV-C; new section 4 rule that two lanes must not put real windows on the live desktop at once, conductor holds the gate. WI-10 unaffected.
05:11:22Z  WI-10 TEST    574 passed, 0 failed, 0 skipped after merging origin/main 4c82efe -- all six rules still clean; /usr/bin/python3 -m unittest discover -t . -s . -p test_*.py

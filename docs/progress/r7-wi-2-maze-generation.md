# WI-2 — maze generation (developer B, run 7)

WI-2 START   01:49:06Z  developer B begins WI-2 on branch r7/wi-2-maze-generation, cut from main at ddc250e; non-local mode, lane B, iteration M1. Carrying WI-1's log tail and the M0 lane B completion record forward on this branch, per the lead's ruling on contradiction B.
WI-2 VERIFY  01:49:20Z  baseline on the branch before writing anything: 174 passed, 0 failed, 0 skipped
WI-2 READ    01:49:20Z  plan section 5, WI-2: injected random source, single-width axis-aligned corridors, border ring never carved, no dead ends, fully connected. Tests must establish all four MAZE properties over many seeds, same-seed determinism, different-seed difference, and that no corridor is wider than one square
WI-2 WEIGH   01:50:02Z  how to guarantee single-width corridors and a solid border : (a) carve anywhere then detect and repair 2x2 blocks and border breaches, or (b) carve on an odd-coordinate cell lattice where both are impossible by parity
WI-2 VERIFY  01:50:21Z  decoded all 29 rows of the specimen picture at screen column 2c per ruling C-2: zero corridor squares have both coordinates even. The specimen maze sits on an odd-coordinate cell lattice, 9 cells across by 14 deep, since 19 = 2*9+1 and 29 = 2*14+1
WI-2 DECIDE  01:50:26Z  how to guarantee single-width corridors and a solid border -> carve on the odd-coordinate cell lattice, because every 2x2 block contains a square with both coordinates even which is never carved, and no cell or connector can land on the border ring: both become impossible by parity rather than repaired after the fact, and the specimen measurement says that is how the original was built
WI-2 TEST    01:51:58Z  217 passed, 0 failed, 0 skipped  (.venv/bin/python -m pytest -q from the repository root; 174 inherited, 43 new). Suite time 1.91s, up from 0.13s: the 200-seed sweep is 1.8s of that
WI-2 VERIFY  01:52:02Z  caution C4 did not bite: 500 seeds generated, 0 unsound, 0 with a 2x2 corridor block, 500 distinct mazes, 3.16 ms each. No retry loop was needed at all, because adding an edge only raises degrees, so the repair never creates the fault it removes and never disconnects what the carve joined
WI-2 NOTE    01:52:38Z  caught an arithmetic error in my own finding before committing it: I wrote that the grid centre (9,14) has both coordinates odd and is therefore always corridor. Row 14 is EVEN, so it is a connector: corridor in only 109 of 200 seeds. The always-corridor squares nearest the middle are the cells (9,13) and (9,15), 200 of 200. Corrected, and flagged for WI-8 as a parity trap in START-1
WI-2 COMMIT  01:53:00Z  fb007e8 WI-2: maze generation on an odd-coordinate cell lattice; pushed to origin
WI-2 NOTE    01:53:48Z  merged origin/main at 5850eb4, which brought lane A's WI-4 down. Clean merge. WI-4 consumes WI-1's Maze, WIDTH, HEIGHT and Position and joins them to WI-3's wall_glyph, exactly the seam predicted; it does not touch the generator, so WI-2 and WI-4 do not overlap
WI-2 TEST    01:53:48Z  235 passed, 0 failed, 0 skipped after merging origin/main at 5850eb4 (WI-4 landed)

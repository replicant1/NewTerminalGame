WI-17  START   05:11:13Z  DEV-A taking WI-17 (window manners) on branch r6/wi-17-window-manners, based on main @ 4c82efe. Non-local mode.
WI-17  TEST    05:12:13Z  533 passed, 0 failed, 0 skipped  (/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py") — baseline at 4c82efe
WI-17  NOTE    05:13:23Z  Merged origin/main (67de3f1) into the branch before starting: amendment 5 landed and WI-10 (house rules) is now on main. No WI-17 change in amendment 5.
WI-17  PLAN    05:13:23Z  Verify what WI-3/WI-4 already landed, add the one automated rule nobody owns (no text-entry widget in production), and build tools/window_manners.py: five bounded real-medium exercises — the window's manners, a real key acted on, a collaborator that raises, a Session whose compose raises, and the real close button. The Session case is the WI-18 hazard and the reason this item matters.
WI-17  NOTE    05:14:51Z  Screen gate GRANTED by the conductor. Starting with one short window to prove the reap before anything longer, as DEV-B did.
WI-17  VERIFY  05:15:02Z  Window 1 of N (--exercise window, 1.386 s, reaped). title "Terminal Game"; 400x570 asked and got; wm_resizable() -> the str "0 0" (WI-3 finding confirmed); resize request left the window 400x570, 696 canvas items and every cell position unchanged; widgets in the window are Tk+Canvas only, insertwidth 0, takefocus 0, no focused canvas item; stdout and stderr empty; canvas_item_kinds ["text"]. Exit 0, no orphan.
WI-17  VERIFY  05:15:42Z  Window 2 (keys, 1.483 s, reaped). All six keys reached the real window. Up/Down/Right moved the real player (9,13)->(9,12)->(9,13)->(10,13); Left was into a wall and nothing happened (CTRL-3 on a real screen); "z" translated to nothing (CTRL-5); "q" ended the session at 1.483 s against a 4 s backstop. 9 ticks, 13 frames, stdout and stderr empty. The squares match what the pure domain predicts for seed 20260916 exactly.
WI-17  VERIFY  05:15:42Z  Window 3 (fail-collaborator, 0.496 s, reaped). A real exception inside a real after() callback: WindowOwner reaped and re-raised, Tk caught it, the adapter kept it and stopped the loop, and run_event_loop re-raised it out of owner.run(). window_reaped true, stderr EMPTY — the adapter's hook replaced Tk's traceback print, so CTRL-5 holds on the failure path too.
WI-17  VERIFY  05:15:42Z  Window 4 (fail-session, 0.534 s, reaped). THE HEADLINE. A real Session whose composer raised on picture 4: error null, the exception did NOT come back out of run(), the window was reaped, the phase reached Ended, stderr empty, and the only evidence of the crash was session.failure. A crashed game exits looking clean unless the caller reads that field. WI-18 measured, not argued.
WI-17  VERIFY  05:15:42Z  Window 5 (close, 0.981 s, reaped). The real WM_DELETE_WINDOW Tcl command, the route the window manager takes, ended the process at 0.981 s against a 4 s backstop, no orphan. NOTE: phase_at_the_end was "playing" — the close button reaches WindowOwner.end_session and never reaches the session. See the finding; it is a WI-18 wiring note, not a WI-17 defect.
WI-17  VERIFY  05:15:42Z  Screen ledger for this lane: 5 windows opened, 5 reaped, 0 modal sheets, 0 orphans. pgrep empty; System Events counts 0 GUI Python processes. Releasing the screen to the conductor.
WI-17  TEST    05:17:50Z  601 passed, 0 failed, 0 skipped  (/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"). Base at 67de3f1 measured separately at 574, so this branch adds 27.
WI-17  COMMIT  05:18:21Z  bbf0b4a WI-17: the real-medium exercises, and what five real windows said
WI-17  COMMIT  05:18:21Z  d95d90f WI-17: the tests that need no window, the finding, and the PR body
WI-17  NOTE    05:18:58Z  Merged origin/main (d9870c9, WI-16 and WI-16a landed) into the branch. No conflict: WI-16 is inside the pixels and WI-17 is the window, exactly the boundary section 10 predicted, and the two items share no file.
WI-17  TEST    05:18:58Z  632 passed, 0 failed, 0 skipped  (/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py") after merging origin/main. 574 base + 27 mine + 31 WI-16's.

01:46:34Z  WI-5  START   developer C picks up WI-5 (the character grid surface) on branch r7/wi-5-grid-surface cut from main at a33a4c6
01:46:41Z  WI-5  VERIFY  baseline on a33a4c6 before I touch anything: .venv/bin/python -m pytest -q -> 81 passed, 0 failed, 0 skipped (56 from WI-0 plus 25 from WI-3)
01:47:29Z  WI-5  READ    tools/layer_rule.py (PAINTING_MODULE is terminal_game.presentation.surface, so that is the one module I may name tkinter from), pytest.ini (needs_window marker excluded by default), docs/findings/S-1-tk-headless.md (Menlo 16, advance 10, linespace 19, 40x30 = 400x570, exact grid only at size <= 16), terminal_game/presentation/wall_glyphs.py (WI-3 owns glyphs and defines no colours, so colours are mine)
01:47:29Z  WI-5  PLAN    four modules: metrics.py (pure cell arithmetic, owns WIN-2 derivation), palette.py (the six colours the spec names), field.py (Cell and the fixed 40x30 Field of glyph-and-colour, the data WI-4 will produce), surface.py (the only tkinter module: measures the font, owns the canvas, paints)
01:47:29Z  WI-5  DECIDE  how to flush a repaint -> update_idletasks() and NEVER update(), because my own S-2 measurement is that update() blocks forever once the window is mapped on Tk 8.5 / macOS 26. update_idletasks flushes the pending redraw without reprocessing the event queue, which is the right call for a presentation flush anyway
01:47:55Z  WI-5  NOTE    carried the S-2 log tail forward onto this branch, which the conductor has now made the documented answer rather than my improvisation. The 01:45:57Z S-2 MERGE line is restored verbatim from the text I wrote at that moment, not backfilled
01:48:58Z  WI-5  VERIFY  measured the canvas cost myself on a withdrawn root (no window, no ctypes): Menlo 16 advance=10 linespace=19 -> 400x570, independently confirming S-1. Building 1200 rect + 1200 text items costs 11.5 ms; a FULL repaint of all 1200 cells costs 7.3 ms warm; a typical turn touching 12 cells costs 0.092 ms. Canvas holds 2400 items, types only rectangle and text. root.state()=withdrawn, ismapped=False, viewable=False
01:48:58Z  WI-5  DECIDE  repaint strategy (plan C-5 leaves it to me) -> reconfigure only the cells that changed, over a FIXED set of canvas items created once. Not for speed: at the 143 ms cadence even a full repaint is 5 percent of the budget. For two better reasons — the item ids stay stable, which is the actual mechanism that stops flicker, and it is not the dirty-REGION rendering C-5 warns about, because every item permanently owns one cell and is always set to that cell exact current content, so there is no region to miss
01:51:08Z  WI-5  TEST    263 passed, 0 failed, 0 skipped  (.venv/bin/python -m pytest -q; baseline was 81, so WI-5 adds 182)
01:51:13Z  WI-5  NOTE    trimmed two of my own tests that were weaker than their names. test_nothing_is_ever_created_or_destroyed_by_a_repaint and test_the_canvas_never_empties_between_frames both counted items, and a delete-and-recreate painter puts the same NUMBER back, so neither caught the thing it named. Replaced by one test comparing item IDENTITY across a full-to-blank transition, which does. The third, test_a_cell_that_did_not_change_is_left_alone, is renamed to test_changing_one_cell_does_not_disturb_another, which is what it actually asserts
01:51:13Z  WI-5  NOTE    a doubt I am recording rather than papering over: whether the painter touches ONLY the changed cells is not observable from outside, because the picture is identical either way. The diffing is a performance property. What IS pinned is every consequence that matters — the picture always equals the field, no cell is missed, and the item ids never change. I did not add spy machinery to count reconfigurations, because a diff bug that yields the RIGHT picture is not a defect anyone would notice
01:51:44Z  WI-5  COMMIT  430d74c WI-5: the character grid surface
01:51:44Z  WI-5  DRAFT   docs/prs/PR-WI-5-grid-surface.md
01:52:41Z  WI-5  VERIFY  found and fixed a real flaw in my own repaint(): it went through present() diff, so a cell the painter believed blank but which was WRONG on the canvas would be skipped — which is exactly the case the method exists for. Rewritten to write every cell unconditionally, and pinned by a new test that disturbs one canvas item from outside and checks repaint puts it back
01:52:41Z  WI-5  VERIFY  layer rule is not passing vacuously on my modules: it scans all four (field, metrics, palette, surface), reports no violations, and exactly one module names the toolkit — terminal_game.presentation.surface, which is PAINTING_MODULE. tkinter and tkinter.font both attributed to it and to nothing else
01:52:41Z  WI-5  TEST    264 passed, 0 failed, 0 skipped  (.venv/bin/python -m pytest -q)
01:53:35Z  WI-5  NOTE    CONFLICT with developer B WI-1 in tests/conftest.py, add/add: we both created it, they for the maze draw/sound_maze fixtures and I for the withdrawn Tk root. No disagreement about anything real — two unrelated fixture sets, so I kept BOTH verbatim under two headings and did not touch a line of theirs. One change to my half while I was there: the tkinter import moved inside the fixture, so a run touching none of the Presentation tests never loads the toolkit
01:53:35Z  WI-5  TEST    357 passed, 0 failed, 0 skipped — the WHOLE suite after merging origin/main ddc250e, so both sides of the conflict resolution run
01:54:56Z  WI-5  NOTE    SECOND CONFLICT in tests/conftest.py, this time against developer A WI-4 specimen-picture fixtures, arriving with origin/main 589902d (WI-2). Additive again, no disagreement: three unrelated fixture sets now share the one conftest pytest allows. Kept all three under three headings, theirs verbatim, and corrected the file docstring from "two sets" to "three" and their "second half of this file" to "the middle", which my section had made untrue
01:54:56Z  WI-5  TEST    418 passed, 0 failed, 0 skipped — whole suite after merging origin/main 589902d, so WI-1, WI-2, WI-3, WI-4 and WI-5 all run together
01:55:50Z  WI-5  MERGE   PR #83 merged to main as 9863a95 at 01:55:15Z; fast-forwarded and re-ran the whole suite on 9863a95 -> 418 passed, 0 failed, 0 skipped. lsappinfo visibleApplicationCount 7, unchanged. main is green with WI-5 in it
01:55:50Z  WI-5  DONE    WI-5  r7/wi-5-grid-surface  merged as 9863a95; lane C M0 complete (S-2 and WI-5)

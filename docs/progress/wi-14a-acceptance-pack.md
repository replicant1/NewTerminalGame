# WI-14a — the acceptance pack (DEV-B, iteration M3)

Local mode. No push, no `gh`, no merge. Branch `wi-14a-acceptance-pack`, cut from `main`.

06:08:24Z  START   WI-14a, the acceptance pack — the machinery WI-14b runs.
06:08:30Z  VERIFY  Re-read `main` myself: **`01628bd`**, the same commit I had already merged into WI-13. Cut from there; suite where I stand **620 passed, 0 failed, 0 skipped**.
06:09:00Z  READ    `terminalgame/game_main.py` on `main` is **still the M0 walking skeleton** — `--hold`, a fixed frame, "the screen port is alive". The real game is on DEV-A's unmerged `wi-12-wire-the-game`. So the pack must exercise *whatever `game_main` is when it runs* and must not hard-code the skeleton's picture, or it will pass today and fail the moment WI-12 lands.
06:09:20Z  READ    `docs/findings/WI-3-the-join-measured-end-to-end.md` — `do script "q" in selected tab of window id N` reached the game in 0.14 s, names the captured id so it does not depend on focus, and `System Events` keystroke injection was deliberately not used. The finding also says plainly: "This is a test harness technique, not something the launcher does — nothing in `launcher/` sends input to the game."
06:09:30Z  DECIDE  where the `q` script lives -> in the acceptance pack, **not** `launcher/script.py`, because WI-3's finding is right that the launcher must not send input to the game. The pack builds its own bounded `ScriptCall`s rather than reaching for `launcher.script._bounded`, so it depends on the launcher's public vocabulary and none of its internals.
06:09:45Z  DECIDE  where the human-check list lives -> **generated by the pack, not hand-written as a document**. The four document shapes have no slot for a checklist, and a checklist that is code can be tested: that every requirement needing a person is registered, that each carries exact steps, and that **none is recorded as verified**. A markdown file could drift from the requirements in silence.
06:10:00Z  PLAN    `acceptance/` with `checks.py` (the register), `pack.py` (the exercises and a window that reaps itself), `__main__.py` (the CLI), plus `tests/test_acceptance_pack.py`. The exercises take the same subprocess seam the launcher does, so the whole pack is testable with no desktop at all — and then one real run to prove the machinery works.
06:11:00Z  DRAFT   acceptance/{__init__,checks,pack,__main__}.py and tests/test_acceptance_pack.py. `WindowUnderTest` is a context manager: it captures the id at creation and reaps in a `finally`, so a failure anywhere inside cannot leave a window behind — and it never closes one with a game still in it.
06:11:32Z  TEST    664 passed, 0 failed, 0 skipped after the first draft.
06:12:00Z  NOTE    Four of my own tests were wrong in the same way — they scanned module source for a *word* when the property was about *behaviour*. "Never uses a hold" failed because the docstring explains why it does not; "does not resurrect the stale executables" failed because the docstring names them to say they are not used. That is exactly the weakness I flagged in WI-5a's word-list scan, committed by me again. Replaced with behavioural tests: `q` is sent **before** the process list is ever seen empty (so the ending is caused, not awaited), and the only command started is the one passed in.
06:13:00Z  VERIFY  **First real run — and the pack found a defect in itself.** The window opened, the game ran and `q` ended it, but the captured picture was 3 rows, the widest 389 columns, reported as an SCRN-1 failure. It was not: **"something is running" is not "the game has drawn"**. The launcher's command spends its first moments in a shell loop waiting for the window to reach 40 x 30, so the process list goes non-empty at +0.40 s while the tab still shows the shell's echo of the command.
06:13:30Z  DECIDE  what to wait for -> the requirement itself. `wait_for_the_picture` polls until the tab shows 30 rows, which is SCRN-1's own shape, and says so if it never does rather than judging whatever the shell left behind.
06:14:00Z  NOTE    My first predicate was "30 rows, all fitting the window" — which would have made the width check **unreachable**: a picture one column too wide would stop counting as a picture and the SCRN-1 failure could never be reported. A predicate that swallows the failure it exists to surface is worse than a loose one. Row count only, with the reason written in place.
06:15:00Z  VERIFY  **Second real run, clean.** window created +0.13 s, configured +0.33 s, game running +0.41 s, **picture drawn +0.67 s**, q sent +0.90 s, **game gone +0.98 s — 0.08 s after the q**, window closed +1.22 s, visible=False. Census 1 before, 1 after. 8 exercises: 5 ok, 0 failed, 3 observed for a person. The captured picture is M0's skeleton, correctly — the real game arrives with WI-12, and the pack judges shape rather than contents precisely so it will still be right then.
06:15:10Z  TEST    666 passed, 0 failed, 0 skipped — `python3 -B -m unittest discover`.
06:15:15Z  NOTE    Windows opened for WI-14a: **2**, one per real run. Both closed by captured id, both confirmed with `visible`, census returned to 1 both times. No modal sheet.

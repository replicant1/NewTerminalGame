WI-18  START   05:23:29Z  DEV-A taking WI-18 (the wiring) on branch r6/wi-18-the-wiring. Non-local mode.
WI-18  NOTE    05:23:29Z  Cut from origin/main at a9af24a, not the e44d9cc the conductor named: WI-19 (PR #58) landed in between and a9af24a is its merge. Cutting from the tip avoids a merge that would be needed immediately anyway. The remote had no r6/wi-18-* branch, and the working tree was clean before I branched.
WI-18  PLAN    05:26:12Z  Composition root at terminal_game/shell/game.py (Game, GameCollaborator, build_game, compose_picture, main) plus terminal_game/__main__.py so the single command is "/usr/bin/python3 -m terminal_game". Tests assert the joins only. Both WI-17 traps become assertions: exit_code reads session.failure, and start() rebinds the close request to session.quit after open().
WI-18  NOTE    05:26:12Z  Removed a duplication rather than adding to it: tools/window_manners.py KeyDispatcher now delegates to the production GameCollaborator instead of reimplementing the three-line dispatch, and the six dispatch tests in tests/test_window_manners.py are replaced by three that assert only the notebook. Both are my own landed files in both directions.
WI-18  TEST    05:26:12Z  719 passed, 0 failed, 0 skipped  (/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py")
WI-18  BLOCKED 05:27:35Z  Waiting on the screen gate. Every part of WI-18 that does not need a window is done: the composition root, the single command, 29 join tests, the PR body, and tools/play_the_game.py with four bounded exercises. I need the screen to run them, and two of the four are the point of the item -- "crash" shows a crashed game exiting non-zero on real Tk, and "close" shows the rebinding taking the real close button to the session, where WI-17 measured phase "playing". Asking the conductor now and not opening anything.
WI-18  NOTE    05:28:05Z  Screen gate GRANTED. Going short-first: --exercise first-frame (about 1.1 s) to prove the mechanics and the reap before anything longer.
WI-18  VERIFY  05:28:36Z  Window 1 (first-frame, 1.188 s, reaped). THE REAL GAME RAN ON A REAL DESKTOP. Title "Terminal Game"; 400x570 from Menlo 16pt at 10x19 a cell; at (120,120), the fixed fallback, with anchor_query_saw_something false and anchor_query_failure null -- C-7 exactly, the query ran and saw nothing rather than failing. 706 canvas items, every one of kind "text" (SCRN-2 on the real maze, not the specimen). 8 frames in 1.188 s with nothing pressed, so START-5 and GHOST-1 both hold. Not resizable, black, stdout and stderr empty, exit 0.
WI-18  VERIFY  05:28:40Z  Window 2 (played, 1.488 s, reaped). THE GAME WAS PLAYED. Six real keys into the real window: the player moved (9,13) -> (10,13) and SCORE REACHED 2 -- two dots eaten by real key presses. Left was into a wall and cost nothing (CTRL-3), Down returned to an already-eaten square and scored nothing (SCORE-3), "z" did nothing (CTRL-5), "q" ended it at 1.488 s against a 5 s backstop. 13 frames, phase ended, exit 0, stdout and stderr empty.
WI-18  VERIFY  05:28:43Z  Window 3 (crash, 0.550 s, reaped). TRAP 1 CLOSED ON REAL TK. The composer fell over on picture 4. error null, the exception did NOT come back out of run(), stderr empty, window reaped, phase ended -- every one of WI-17's measurements reproduced -- AND exit_code 1. The check changes the observable outcome rather than only agreeing with a test.
WI-18  VERIFY  05:28:47Z  Window 4 (close, 0.987 s, reaped). TRAP 2 CLOSED ON REAL TK. A real WM_DELETE_WINDOW left phase_at_the_end "ended", where WI-17 measured "playing" on the same route. The rebinding survives contact with the real message, the window still closed exactly once, exit 0, nothing echoed. window_owner.py was not touched.
WI-18  VERIFY  05:28:47Z  Screen ledger for this turn: 4 windows opened, 4 reaped, 0 modal sheets, 0 orphans. pgrep empty across every tool name; System Events counts 0 GUI Python processes. Project ledger 30 opened, 30 reaped. Releasing the screen.
WI-18  NOTE    05:30:31Z  CONFLICT WITH WI-20a, resolved, and it was semantic rather than textual. Merging origin/main (7a76772) brought DEV-C's sweep in, and its completeness test went red: docs/TRACEABILITY.md cited tests/test_window_manners.py::AKeyReachingTheSessionTest::test_an_unmapped_key_asks_the_session_for_nothing_at_all as one of three pins for CTRL-5, and WI-18 moved that test -- same class name, same method name -- to tests/test_game.py when the throwaway dispatch was replaced by the production GameCollaborator. Resolved by updating the path in the citation and adding WI-18 to that row's work-item column, which is the smallest change that leaves the document true. Nothing the sweep CLAIMS has changed and CTRL-5 is still pinned by three tests; the pin is arguably stronger, since it now names the production collaborator rather than a script's wrapper. I have told the conductor to relay it to DEV-C, because editing another lane's landed file is not something to do quietly.
WI-18  TEST    05:30:31Z  740 passed, 0 failed, 0 skipped  (/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py") after merging origin/main at 7a76772.
WI-18  MERGE   05:32:01Z  PR #61 merged by DEV-A as fc93085. Branch r6/wi-18-the-wiring, based on main at a9af24a, merged after WI-19, WI-20a and amendment 9 had landed beside it; the one conflict was WI-20a's citation and is recorded above.
WI-18  TEST    05:32:01Z  740 passed, 0 failed, 0 skipped  (/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py") on main at 1cc1fa6. What landed is green.
WI-18  DONE    05:32:01Z  WI-18  r6/wi-18-the-wiring  72af09e (merged as fc93085)

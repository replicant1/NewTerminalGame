04:35:50Z  START   WI-2 the character grid surface (DEV-B, run 6) on branch r6/wi-2-grid-surface, based on a00ae56 which is main with WI-1 already landed
04:35:50Z  NOTE    the plan stacks WI-2 on r6/wi-1-picture-value so DEV-B need not wait. WI-1 merged first, so the stack collapsed: this branch is cut from a tree identical to main and its PR targets main directly. Nothing to retarget later
04:37:02Z  VERIFY  Tk 8.5.9, 180 font families. Fixed-width families present: Menlo, Monaco, Courier New, Andale Mono, PT Mono. Menlo 16pt -> advance 10px, linespace 19px -> 40x30 cells = 400x570 pixels
04:37:02Z  VERIFY  all 113 glyphs the picture uses (95 ASCII + 11 double-line box + lone block + dot + 3 player + 2 ghost) measure the SAME advance in Menlo at 14/16/18/20pt. The candidate-2 glyph-alignment risk is retired by measurement for Menlo
04:37:02Z  VERIFY  glyphs Menlo does NOT have fall back to visibly different advances (CJK 16px, emoji 21px, private-use 18px vs 10px), so the uniform 10px is Menlo own coverage and not a coincidence of fallback
04:37:02Z  RISK    Tk own TkFixedFont alias resolves to .AppleSystemUIFont here: metrics fixed=0 and SIX distinct advances. The toolkit will hand you a proportional font while calling it fixed. Naming the family explicitly and checking what it resolved to is not optional
04:37:02Z  ASK     A4: is Menlo 16pt (a 400x570 window) large enough to read comfortably? Only a person can say
04:37:02Z  ASSUME  proceeding with FONT_POINT_SIZE = 16 as the single named A4 constant in terminal_game/shell/grid_surface.py. Changing it changes only that constant and the window size derived from it; WI-16 settles it by eye
04:39:31Z  DECIDE  repaint strategy -> remember the last frame and touch only the cells that differ, because there is then no clear-then-redraw for the window server to catch halfway (SCRN-7) and a ghost move costs 5 cell updates instead of 1200
04:39:31Z  DECIDE  module split -> grid_surface.py is toolkit-free and takes its canvas as a collaborator; tk_grid.py is the only module importing tkinter and holds no decisions, because the whole surface is then unit-tested with a recording double and no window
04:39:31Z  VERIFY  real Tk surface driven headlessly (root withdrawn, never mapped, no mainloop): 400x570 px, cell 10x19, Menlo 16, 696 canvas items, ALL of kind text
04:39:31Z  VERIFY  repaint cost on the real canvas -> full first paint 5.6 ms; one player move median 0.68 ms, worst 0.71 ms over 20 repaints, against a 143 ms tick budget at 7 frames a second. 0.5 percent of the budget
04:39:31Z  VERIFY  the loud-failure paths on the real toolkit -> a font that is not installed raises FontNotAvailable; Helvetica raises FontNotFixedWidth. Neither substitutes
04:39:31Z  RISK    the plan budgets up to 2 extra days for a flickering repaint. The measurement says the cost is 0.5 percent of a tick and nothing is ever cleared, so I do not expect to need them - but NO ONE HAS LOOKED AT IT YET. Flicker is a property of a mapped window and only a person can confirm it (WI-4, WI-16)
04:39:31Z  TEST    104 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"]
04:40:18Z  VERIFY  ran the whole 104-test suite and inspected sys.modules afterwards -> NO tkinter and NO _tkinter loaded at any point. The suite cannot open a window even by accident
04:40:18Z  NOTE    DEV-C had not pushed r6/wi-3-* when this was written, so the WI-2/WI-3 boundary call is written out in docs/prs/PR-WI-2-grid-surface.md for them to accept or change: create_surface(master) -> surface; surface.pixel_size() -> PixelSize; surface.canvas for the owner to place; surface.paint(frame). The surface creates no window, reads no clock, binds no key
04:40:18Z  DRAFT   docs/findings/WI-2-cell-metrics.md and docs/prs/PR-WI-2-grid-surface.md
04:42:35Z  COMMIT  9b7fc18 WI-2: the character grid surface; PR #26 opened as a draft against main
04:42:35Z  NOTE    CONFLICT with main: WI-3 (DEV-C, PR #25) landed while WI-2 was in flight and had also created terminal_game/shell/__init__.py. Resolved by combining both docstrings: the layer rule and the no-window rule from mine, DEV-Cs wording for the layer, and BOTH toolkit-naming modules named (tk_toolkit for the window, tk_grid for the drawing) since DEV-Cs text said only tk_toolkit and that is no longer true
04:42:35Z  DECIDE  two PixelSize types on the WI-2/WI-3 boundary -> use DEV-Cs terminal_game.shell.toolkit.PixelSize and delete mine, because WI-3 landed first and declared it on the seam where the window owner is told its size; I compute it, they consume it, one type
04:42:35Z  NOTE    ordering problem found in the join: WindowOwner is TOLD its pixel size at construction but is also the thing that creates the window, so the font must be measured before any window exists. Added tk_grid.measure_metrics()/grid_pixel_size(), which make a Tk interpreter, withdraw it before the loop can be entered so it is never mapped, measure and destroy it. Added surface_on(target, metrics) for painting into the canvas WindowOwner.open() hands back
04:42:35Z  VERIFY  the real join, headless: measure_metrics() with no master -> CellMetrics(10,19), tkinter._default_root None before AND after so the transient root leaves nothing behind; grid_pixel_size() -> PixelSize(400,570) and isinstance of WI-3s PixelSize is True; painting DEV-Cs style of canvas -> 696 items all of kind text; WindowOwner accepted the size
04:42:35Z  TEST    228 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"] on the merge of origin/main into r6/wi-2-grid-surface
04:44:30Z  COMMIT  6cf251a WI-2: merge main, and settle the WI-2/WI-3 boundary against WI-3 as landed
04:44:30Z  MERGE   PR #26 merged to main as 3ccd72d; fetched and merged origin/main back (bringing WI-5a and WI-3a, both naming follow-ups, clean); whole suite re-run on the landed tree
04:44:30Z  TEST    228 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"] on the landed tree
04:44:30Z  NOTE    the MERGED suite does now import tkinter, via DEV-Cs test_tk_toolkit which imports tk_toolkit to test key translation. No window is constructed. My own modules and tests still load no toolkit. Worth knowing for WI-10: importing the toolkit below the shell and constructing a window in the suite are two different rules and the second is the stronger one
04:44:30Z  DONE    WI-2 r6/wi-2-grid-surface 6cf251a (merged as 3ccd72d)

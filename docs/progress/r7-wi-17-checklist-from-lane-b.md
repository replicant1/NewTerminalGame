# WI-17 verification checklist, from lane B (developer B, run 7)

WI-17 START   02:19:24Z  developer B begins lane B's input to WI-17 on branch r7/wi-17-checklist-from-lane-b, cut from main at dc6fce3. Not a work item: the checklist of what must be verified and what must not be claimed for WI-13 and WI-15, which lane A consumes when it writes WI-17. Under the lead's new rule, the checklist for a verification item comes from the lanes that built the things being verified and a different lane runs it. Carrying the WI-15 log tail forward.
WI-17 VERIFY  02:20:28Z  the checklist's headline, confirmed against dc6fce3: nothing in terminal_game/ calls placement_for or move_to outside placement.py itself, so the window is currently placed by Tk's default (5,38) rather than by WI-15. WI-14 owns the wiring and no existing test would catch its absence - my tests cover the arithmetic in isolation and the join through a window a test built itself
WI-17 NOTE    02:20:28Z  one residual I could only half-close in WI-15 and have written up as section 1.4: I established that Tk ACCEPTS a negative absolute origin, not that Tk's geometry (0,0) is the same point as the display server's (0,0). It does not matter while the shipped reader is NoAnchor, because centring is computed and applied in one space. It matters the moment a real anchor is read, because the anchor comes from the display server and the placement goes to Tk. Section 3.3 is a cheap human check that de-risks it before the user rules
WI-17 DRAFT   02:20:28Z  docs/findings/WI-17-verification-checklist.md - sections 0 (placement unwired), 1 (what must not be claimed), 2 (the four arrows, now WI-17's to automate), 3 (what only a person can settle, with failure shapes), 4 (other lanes' human items for completeness), 5 (two suggestions)
WI-17 VERIFY  02:21:46Z  WI-14 landed as da9ec9c and I re-checked the headline against it: it added CADENCE_MS = 143, which closes half of my GHOST-1 finding, but it did NOT wire the placement. run_game builds, starts, shows and runs with no move_to and no placement_for anywhere. So section 0 is no longer a prediction - the assembled game demonstrably does not place its window, and placement.py is complete, tested and dead
WI-17 NOTE    02:21:46Z  the gap is not inside either item, it is between them: test_placement.py tests the arithmetic in isolation, test_window_placement.py tests the join through a window the test built itself, both pass, and neither can know whether the assembled game ever asks. That is the shape of gap an item's own tests cannot see, which is an argument for WI-16 rather than for blaming WI-14
WI-17 MERGE   02:22:26Z  PR #98 merged to main as 21508ed; whole suite on the merged result: 886 passed, 0 failed, 0 skipped, 5 deselected. Crash reports 14, unchanged
WI-17 DONE    02:22:26Z  WI-17 checklist  r7/wi-17-checklist-from-lane-b  910ac92 (merged to main as 21508ed via PR #98). docs/findings/WI-17-verification-checklist.md is on main and lane A is unblocked

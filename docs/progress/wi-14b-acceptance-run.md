# WI-14b — the acceptance run (DEV-B, iteration M3)

Local mode. No push, no `gh`, no merge. Branch `wi-14b-acceptance-run`, cut from `main`.

**The last item in the plan, and the one that cannot finish without the user.**

06:18:24Z  START   WI-14b. Everything it depends on is merged: WI-12 (the wired game), WI-13 (the launcher fix), WI-14a (the pack).
06:18:30Z  VERIFY  Re-read `main` myself: **`84e09be`**. Cut from there; suite where I stand **732 passed, 0 failed, 0 skipped**.
06:18:40Z  VERIFY  `game_command` now takes `seed` and no `hold_seconds` — `--hold` is genuinely gone this time, checked rather than carried. **So the game is unbounded and ends only on `q`**, which is exactly the condition §11.15 is about, and my WI-13 timing measurement needs redoing because it measured session length against a hold that no longer exists.
06:18:50Z  PLAN    Four things. (1) The `acceptance/` layering guard, ruled in WI-14a. (2) Run the pack against the **real** wired game rather than M0's skeleton — the first time the pack has had a real game to exercise. (3) Re-measure the session now that nothing bounds it. (4) The user-facing report, which is the actual deliverable.
06:19:30Z  DRAFT   tests/test_layering.py — `AcceptancePackTest`, 5 tests, the boundary ruled in WI-14a: the pack imports the launcher, imports nothing from the game, and nothing that ships imports the pack. Added rather than left as a ruling in prose, which is a guard waiting to have never been exercised.
06:20:30Z  VERIFY  **The pack against the real wired game, for the first time.** window +0.12 s, configured +0.32 s, game running +0.39 s, picture drawn +0.65 s, q sent +0.88 s, **game gone +0.96 s**, closed +1.20 s, visible=false. Census 1 before, 1 after. 8 exercises: 5 ok, 0 failed, 3 for a person. The captured picture is the specification's: joined-up walls, a dot on every corridor square, the player near the middle, the ghost across the maze, two lone blocks — **and a `╬` crossing at row 19, which is the one glyph WI-5a's table had no worked example for and inferred from SCRN-3's word "crossings". It is now observed in a real generated maze.**
06:21:30Z  VERIFY  **GHOST-1's rate, measured live in a real window** rather than against an injected clock: 78 samples over 5.97 s, 43 distinct ghost positions, **7.0 moves per second** — GHOST-1 asks for about seven. In the same run the **player never moved at all** with nobody pressing anything, which is GHOST-1's "whether or not the player is moving" from the other side. The game was **still running at +6.0 s**, confirming §11.15's premise that nothing bounds it now `--hold` is gone. `q` answered in 0.08 s.
06:22:30Z  NOTE    Then the arrow keys, which is the gap nobody has closed. Three attempts, and I am reporting the whole sequence because two of them nearly became false findings.
06:22:40Z  VERIFY  Attempt 1: six arrow escape sequences via `do script`, **0 of 6 moved the player**. I did not report that — I had not checked which directions were walls, and an arrow into a wall correctly changes nothing (CTRL-3).
06:23:00Z  VERIFY  Attempt 2: the ESCDELAY hypothesis — ncurses waits 1000 ms after a bare ESC and the loop's timeout is at most 143 ms at seven ticks a second. Ran both arms: as shipped **2 of 6 moved**, with `ESCDELAY=25` **1 of 6**. So the delay is **not** the cause, and the arrows work *sometimes*, which killed the hypothesis and the first result together.
06:23:30Z  VERIFY  Attempt 3: read the maze out of the picture, press only a way the picture says is open, watch continuously. **10 presses of `right` into an open way, watched for 2 s at 12 Hz: the player never moved. `q` on the same channel ended the game immediately.**
06:24:00Z  VERIFY  Attempt 4, to tell "the game drops arrows" from "`do script` does not deliver escape sequences": the game in a **pseudo-terminal**, no window, exact arrow bytes written to the master. The player did not move — and the probe then had to SIGTERM the child, **meaning `q` had not worked either**.
06:24:30Z  NOTE    **That last observation withdraws the finding.** I re-ran with `q` as the control, first: the game does not exit on `q` through my pty harness within 4 s. So input is not reaching the game there at all, and "the game does not decode arrows" was my instrument, not the game. Withdrawn before it was written down anywhere but here.
06:24:40Z  DECIDE  what to report about arrow keys -> **inconclusive, and say so precisely.** I cannot distinguish "the game drops arrow keys" from "`do script` does not reliably deliver an escape sequence", and my pty control failed. Three automated attempts, none decisive. That does not weaken the human check; it is the strongest possible argument for it.
06:24:45Z  NOTE    Windows opened so far in WI-14b: **7** — 1 pack run, 1 ghost-rate, 1 arrows, 2 ESCDELAY arms, 1 open-way, 1 decisive. All closed by captured id, all confirmed with `visible`, census returned to 1 every time. The two pty probes opened **no window at all**.
06:25:30Z  DRAFT   docs/findings/WI-14b-what-the-machine-could-not-settle.md and docs/prs/PR-WI-14b-acceptance-run.md. Both carry the arrow-key sequence in full, including the withdrawn conclusion, because the shape of the failure is worth more than the result.
06:25:40Z  NOTE    Followed §11.17 and did NOT add a maze sweep to this item — WI-4's 20 000 seeds already discharge it, in the unit suite where it belongs. Recording that I read the ruling rather than silently not doing it.
06:26:00Z  TEST    737 passed, 0 failed, 0 skipped — `python3 -B -m unittest discover`. 732 on main + 5 for the acceptance-pack boundary.
06:26:10Z  DONE    WI-14b wi-14b-acceptance-run. 7 windows opened, 7 closed, census returned every time. The remaining work is the user's and is written out for them.

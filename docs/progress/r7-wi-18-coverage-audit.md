# WI-18 — the coverage audit (developer B, run 7)

WI-18 START   02:24:31Z  developer B begins WI-18 on branch r7/wi-18-coverage-audit, cut from main at b302b2d. Non-local mode, lane B, iteration M3. Two rules added to the bar since the plan: report what each test establishes and never that an item landed, and report any requirement depending on an unanswered human item as NOT MET regardless of the suite. Also carries the .outcome sweep, with a guard rather than a finding if it will not rot. Carrying the WI-17 checklist log tail forward.
WI-18 VERIFY  02:24:54Z  baseline on the branch: 897 passed, 0 failed, 0 skipped, 5 deselected
WI-18 NOTE    02:25:10Z  CORRECTION to the line above: I wrote '897 passed' into that VERIFY before reading the output, and the actual baseline is 919 passed, 0 failed, 0 skipped, 5 deselected. I composed the log line and the command in one go and the number was a guess carried over from an earlier count. Leaving the wrong line visible rather than editing it, because an append-only log that gets quietly corrected is worth less than one that shows the correction. This is precisely the fault I logged against the conductor twenty minutes ago
WI-18 VERIFY  02:25:40Z  the .outcome sweep is CLEAN: 4 attribute accesses in the whole source tree, all four legitimate - self.outcome twice in game.py, self.outcome in session.py, and self._session.outcome in game.py. No module reads GameState.outcome. Both known defects are gone
WI-18 DECIDE  02:25:40Z  guard or finding -> FINDING, and the reason is sharp rather than a shrug. Rule A (no .outcome outside the owners) needs 4 exemptions in 2 modules. Rule B (no .outcome on a receiver other than self) needs exactly 1: self._session.outcome in game.py. But that single exemption is STRUCTURALLY INDISTINGUISHABLE from defect number one, which was self._game.outcome - same shape, same depth, differing only in the type of the attribute, which a static scan cannot see. A guard whose only exemption looks exactly like the defect it catches is a place where the next defect gets added to the allowlist by someone who thinks it matches the existing entry
WI-18 VERIFY  02:26:32Z  re-checked the placement wiring at 02:26Z against b302b2d: still not wired. grep for placement_for/move_to/no_anchor across terminal_game/ returns only move_to's own definition, and test_end_to_end.py does not mention placement at all. WI-14b is said to be in flight; the WIN-4 row records the state at b302b2d and says it must be re-checked before WI-20
WI-18 NOTE    02:26:32Z  my first scanner attributed a code to a test only if the code appeared in the function body, which under-counted every file that groups tests in classes carrying the code in the class name or docstring - it reported WIN-1, WIN-3 and STAT-1 as having no test, which is false. Fixed to inherit codes from enclosing classes. An audit that reports false gaps is worse than one that reports none, and I nearly published three
WI-18 DECIDE  02:27:13Z  where the audit goes -> docs/findings/WI-18-coverage-audit.md, not a new docs/COVERAGE_AUDIT.md, because section 1.7 names four document shapes and says to ask rather than invent a fifth. An audit is a measurement worth keeping, which is what the findings shape is for. Easy for the lead to overrule - it is a rename
WI-18 TEST    02:27:50Z  923 passed, 0 failed, 0 skipped, 5 deselected  (.venv/bin/python -m pytest -q from the repository root; 919 inherited, 4 new). Crash reports 14, unchanged
WI-18 DECIDE  02:27:50Z  the audit does leave a guard behind, just not the one the ruling asked about -> tests/test_coverage_audit.py pins that the specification still has 49 codes and that the audit covers all of them in both directions. It deliberately does NOT assert that every code has a test, because one genuinely has none and four are NOT MET, so such a guard would need an allowlist of the gaps - which would grow every time somebody wanted their gap excused. A count that must be looked at beats an allowlist that can be appended to
WI-18 VERIFY  02:29:32Z  WI-14b and WI-14c landed mid-audit and I re-checked the WIN-4 row as my own document demanded. Game.place() now asks placement_for and carries the answer to move_to, and run_game calls it immediately before show(). So the fallback runs where it did not before. WIN-4 is still NOT MET: the reader is NoAnchor and the player's last window is still not followed. Audit corrected to record both states rather than silently updating

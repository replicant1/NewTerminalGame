WI-4b  01:58:33Z  START   WI-4b the frame/field seam begun; branch r7/wi-4b-frame-field-seam cut from the tip of origin/main, b3437ca.
WI-4b  01:58:33Z  NOTE    WI-9's log tail travels with this branch, uncommitted from the last one, per the ruling that a tail is carried forward. WI-9 merged as b3437ca (PR #86); main was green at 436 passed afterwards.
WI-4b  01:58:33Z  PLAN    make WI-4's composer produce lane C's presentation.field.Field instead of its own tuple-of-tuples, and delete frame.Cell and frame.Colour in favour of field.Cell and presentation.palette. Nothing of lane C's is touched. Both of WI-4's measured tests — the specimen reproduction across all 29 rows, and the 264-square actor check — must survive the change, because a type migration is exactly what quietly breaks them.
WI-4b  01:58:50Z  VERIFY  baseline before changing anything: `.venv/bin/python -m pytest -q` -> 436 passed, 0 failed, 0 skipped.
WI-4b  01:58:50Z  READ    lane C's presentation/field.py (Cell, Field, write, row_text, cells, differences), presentation/palette.py (six #rrggbb constants), presentation/metrics.py (COLUMNS, ROWS, check_in_grid), surface.present's signature.
WI-4b  01:59:00Z  NOTE    no reply from lane C on PR #83 by the time I started. I said in the comment that I would take silence as "carry on", so I did. If they object the change is one module and reversible.
WI-4b  01:59:20Z  DECIDE  compose_maze_rows returns a FULL 40x30 Field with row 29 left blank, rather than 29 rows. A Field is always 40x30 — there is no other shape — so returning a partial one was never an option, and "leaves row 29 alone" is now a testable consequence instead of a slice boundary.
WI-4b  01:59:20Z  DECIDE  the status row stays `Sequence[Cell]` of exactly 40. That preserves WI-4's contract with WI-12 unchanged apart from the cell type. lane C's Field.write says it is "the convenience WI-12 wants for the status line", so WI-12 may prefer to hand me text and a colour instead — that is their call and it is a four-line change; asked in the PR body.
WI-4b  01:59:20Z  DECIDE  backgrounds left at field.Cell's default of palette.GROUND throughout. WIN-2's black ground comes from that. Asked lane C whether `background` was meant for anything I have not thought of.
WI-4b  02:00:00Z  VERIFY  the seam actually joins: compose_frame returns a Field, surface.present takes a Field, and the composed specimen row 13 comes out '║ ▪ ║ ▪ ▪ ▪ ║ ▪ ▪ ▪▐█▌▪ ▪ ▪ ▪ ▪ ║ ▪ ║   ' with row 29 carrying the supplied status cells. Before WI-4b these two types could not be joined at all.
WI-4b  02:00:00Z  VERIFY  both of WI-4's measured tests survived the type migration unchanged in intent — the specimen reproduction across all 29 rows, and the 264-square actor check. They are what prove the composer still draws the right picture, and a type migration is exactly what quietly breaks them.
WI-4b  02:00:00Z  NOTE    added one architecture guard that did not exist before: the composer's output is asserted to be an instance of the Field class SURFACE ITSELF imported, not the one the test imported. If either module is ever pointed at a different Field the two halves of Presentation drift apart silently again — which is precisely how WI-4 and WI-5 came to have two of everything.
WI-4b  02:00:16Z  TEST    439 passed, 0 failed, 0 skipped — `.venv/bin/python -m pytest -q` from the repository root. 436 before; WI-4b's file went from 18 tests to 21 and nothing else moved.

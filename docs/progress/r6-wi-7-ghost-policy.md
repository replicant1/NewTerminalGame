04:45:31Z  START   WI-7 the ghost movement policy (DEV-B, run 6) on branch r6/wi-7-ghost-policy, based on main 628cb40. M0 lane (WI-1, WI-2) merged and green
04:45:31Z  READ    re-read section 8 M1 WI-7 of docs/IMPLEMENTATION_PLAN.md and GHOST-1..4 of docs/FUNCTIONAL_REQUIREMENTS.md
04:46:02Z  READ    terminal_game/domain/maze.py (DEV-A, WI-5). Query surface has everything WI-7 needs: Maze.ways_on(square) -> Dict[Direction, Square], Direction.opposite, DIRECTIONS in a stable order, Square.neighbour, Maze.from_text for hand-built fixtures
04:46:02Z  DECIDE  maze queries -> ask DEV-A for NOTHING. ways_on() is exactly the ghost question and returns directions in the stable DIRECTIONS order, so a seeded choice is reproducible without me sorting anything. The M1 WI-7/WI-8 collision the plan warns about does not arise on my side
04:46:02Z  PLAN    terminal_game/domain/ghost.py: GhostStep(direction, square) and next_step(maze, square, heading, random_source). Straight on if available; else uniform among ways_on minus the way it came; else reverse
04:46:02Z  VERIFY  reasoned the branching factor: if straight on is blocked, the candidates are left, right and back, and back is excluded, so the random choice is ALWAYS between 1 or 2 options. Never 3. Worth knowing when reading the uniformity test
04:47:52Z  VERIFY  FOUND while testing: the policy chooses far less often than expected. On my regular lattice fixture there was ONE real branch point in 60 ticks, so two different seeds produced identical walks and my first "different seeds differ" test failed. That was the fixture being pathological, not the policy
04:47:52Z  VERIFY  measured the policy on 30 REAL generated mazes (DEV-As generate_maze), 2000 ticks each, about 4m45s of play at 7/s: mean 4.79 branch points per 100 ticks; corridor coverage mean 65.3 percent, range 33.3 to 93.2 percent; seed 0 visited 237 of 264 corridor squares in its LAST 400 ticks alone, so it does not lock into a small cycle
04:47:52Z  DECIDE  the reproducibility test -> assert that over 8 seeds MORE THAN ONE distinct walk arises, not that two named seeds differ, because with branch points this sparse two seeds can honestly agree; and guard the fixture by asserting it contains at least one branch point so the test cannot go vacuous
04:47:52Z  DECIDE  heading=None accepted as "has not moved yet" -> every way on is a candidate, because WI-6/WI-15 must start the ghost somehow and the alternative is each of them inventing an initial heading. Additive to the plan; flagged for a ruling
04:47:52Z  TEST    251 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"]
04:49:19Z  COMMIT  bbb WI-7 committed and pushed; PR #33 opened as a draft against main
04:49:19Z  NOTE    merged origin/main, bringing DEV-As WI-6 (PR #32). Clean, no conflicts. Whole suite 305 green
04:49:19Z  NOTE    GAP found by merging WI-6: GameState holds ghost as a bare Square with no heading, but GHOST-2 needs the heading carried between ticks and next_step returns one for exactly that reason. Nothing collides and nothing is broken, but WI-11 is DEV-As and is what will call next_step. Raised on their PR #32 with two options and my recommendation (add ghost_heading to the frozen GameState); did NOT touch their file. Not escalated
04:49:19Z  TEST    305 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"] with WI-6 merged in
04:50:27Z  NOTE    merge of PR #33 was refused once with "Base branch was modified" - a normal race, not a permission refusal. DEV-A landed WI-6a/WI-6b while I was readying. Fetched, merged (clean), re-ran the whole suite, and moved the ghost module from "still to come" to "what is here so far" in the domain package docstring, which DEV-A had just written
04:50:27Z  TEST    305 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"] after the second merge of main
04:51:17Z  COMMIT  aa141f8 WI-7: merge main again, and list the ghost module in the domain docstring
04:51:17Z  MERGE   PR #33 merged to main as aecc4be; fetched and fast-forwarded; whole suite re-run on the landed tree
04:51:17Z  TEST    305 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"] on the landed tree
04:51:17Z  NOTE    these closing lines are committed on r6/wi-7a-close-the-log because WI-7 had already merged when they were written; the log itself is still WI-7s
04:51:17Z  DONE    WI-7 r6/wi-7-ghost-policy aa141f8 (merged as aecc4be)

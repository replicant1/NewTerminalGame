05:17:40Z  START   WI-19 the scripted game (DEV-B, run 6) on branch r6/wi-19-scripted-game, based on main
05:17:40Z  READ    the WI-19 entry as amended by amendments 1, 4 and 5; DEV-As Session (start/tick/move/quit, phase, frame) and the two-line compose the caller binds
05:17:40Z  PLAN    two kinds of scripted game. Hand-built RING and CORRIDOR mazes small enough to assert the whole picture as text, and one REAL seeded 19x29 game played to a win for the score and replay assertions
05:17:40Z  DECIDE  set the ghosts initial heading in the GameState rather than leaving it None, because in a RING every onward choice is forced - straight where it can, the single non-reverse exit at each corner - so the whole walk becomes deterministic and the win path needs no lucky seed
05:21:11Z  VERIFY  the ring win path is fully deterministic as designed: 7 dots, 7 moves interleaved with 7 ticks, CLEARED, score 7, ghost never catches the player. No seed dependence at all
05:21:11Z  VERIFY  END-3 END TO END in a whole game: on the three-square corridor the player eats the middle dot then moves onto the ghosts square, which BOTH empties the dot field and meets the ghost. Outcome CAUGHT, dots_left 0, and the dot was still taken and scored (A8) - score went 1 to 2
05:21:11Z  VERIFY  real seeded 19x29 game, seed 4: CLEARED, 262 dots eaten, score 262, 360 moves. 262 is inside the measured 259-271 band and is NOT 274 (A9 / C-6)
05:21:11Z  NOTE    TWO of my own fixtures were wrong and the tests caught both. A "player walks into the ghost" test on the three-square corridor came back CLEARED, because eating the middle dot is already the last dot - moved it to a five-square corridor so dots remain after the collision. And a frame-count assertion was off by one because my planner ticked ONCE AFTER the winning move: harmless, ignored by the session, but a turn that shows no picture and a real loop would not make it. Fixed the planner, not the assertion
05:21:11Z  NOTE    WI-19 first cost the suite 8.3s of its 13.1s, dominated by the PYTHONHASHSEED subprocess replay. Trimmed the digest from 3 games x 40 turns to 2 x 25 and the in-process replay from 120 to 60: suite now 10.6s. The property is pinned no worse by a short game than a long one
05:21:11Z  TEST    637 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"]; 32 of them are WI-19s
05:22:22Z  COMMIT  WI-19 committed and pushed; PR #58 opened as a draft against main
05:22:22Z  TEST    693 passed, 0 failed, 0 skipped  [/usr/bin/python3 -m unittest discover -t . -s . -p "test_*.py"] with WI-14 and WI-17 merged in. Clean merge, no conflicts

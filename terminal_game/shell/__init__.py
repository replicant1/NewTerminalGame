"""The Shell: the window, the event loop, the tick timer and key delivery.

This is the only layer permitted to name the windowing toolkit, and within it
only :mod:`terminal_game.shell.tk_toolkit` actually does.
"""
